<!DOCTYPE html>
<html lang="en">r deploying to seed Netlify Blobs with your existing data.
<head>
  <meta charset="UTF-8" />/blobs
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GigHub – Dashboard</title>ersonal-access-token
  <link rel="stylesheet" href="/css/gighub.css" />
  <!-- Remove the animations.js reference if the file doesn't exist -->
  <!-- <script src="../js/animations.js"></script> -->
</head>ath = require("path");
<body>{ getStore } = require("@netlify/blobs");

<script>TA_DIR = path.join(__dirname, "..", "data");
// Restrict access unless signed in
if (!localStorage.getItem('gighub_user')) {
  window.location.href = 'login.html';
}   name: "gighub-data",
</script>D: process.env.NETLIFY_SITE_ID,
<div class="dash-shell" id="dashShell" data-role="freelancer">
  });
  <!--- ═══════════════════════════════════════════
       SIDEBAR= ["profiles", "jobs", "messages", "reviews", "notifications", "services"];
       ═══════════════════════════════════════════ --->
  <aside class="dash-sidebar" id="sidebar">
    const filePath = path.join(DATA_DIR, `${name}.json`);
    <div class="dash-sidebar__brand" style="cursor:pointer;" onclick="document.querySelector('[data-page=dashboard]').click();">
      <img src="../Images/GIGHUBLOGO ONLY.PNG" alt="GigHub" class="dash-sidebar__logo"
        onerror="this.style.display='none';document.getElementById('sb-fb').style.display='flex';"/>
      <div id="sb-fb" class="dash-sidebar__logo-fb" style="display:none;">
        <svg width="22" height="22" viewBox="0 0 80 80" fill="none">
          <polygon points="40,10 44,24 58,24 47,33 51,47 40,38 29,47 33,33 22,24 36,24" fill="#f5a623"/>
        </svg>
      </div>
      <span class="dash-sidebar__name">GigHub</span>
    </div>

    <nav class="dash-nav" id="dashNav">      <button class="dash-nav__item active" data-page="dashboard" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/>          <rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/>        </svg>        Dashboard      </button>      <button class="dash-nav__item" id="navServices" data-page="services" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/>        </svg>        <span id="navServicesLabel">My Services</span>      </button>      <button class="dash-nav__item" data-page="profile" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>          <circle cx="12" cy="7" r="4"/>        </svg>        Profile      </button>      <button class="dash-nav__item" data-page="messages" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>        </svg>        Messages      </button>      <button class="dash-nav__item" data-page="settings" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <circle cx="12" cy="12" r="3"/>          <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/>        </svg>        Settings      </button>    </nav>    <div class="dash-sidebar__footer">GigHub</div>  </aside>  <!-- ═══════════════════════════════════════════       MAIN       ═══════════════════════════════════════════ -->  <div class="dash-main">    <!-- Topbar -->    <header class="dash-topbar">      <div class="dash-topbar__user">        <div class="dash-topbar__avatar">          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#1b3d7a" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>          </svg>        </div>        <span class="dash-topbar__username">Jane Doe</span>      </div>      <div class="dash-topbar__right">        <button class="dash-change-role" onclick="openRoleModal()">Change Role</button>        <div style="position:relative;">          <button class="dash-notif-btn" id="notifBellBtn" onclick="toggleNotifDropdown()">            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">              <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>              <path d="M13.73 21a2 2 0 0 1-3.46 0"/>            </svg>            <span class="dash-notif-badge" id="notifBadge" style="display:none;"></span>          </button>          <!-- Notification Dropdown -->          <div id="notifDropdown" style="display:none;position:absolute;top:calc(100% + 8px);right:0;width:340px;max-height:420px;background:#fff;border-radius:14px;box-shadow:0 8px 40px rgba(0,0,0,0.18);z-index:9999;overflow:hidden;font-family:Poppins,sans-serif;">            <div style="display:flex;align-items:center;justify-content:space-between;padding:16px 18px 12px;border-bottom:1.5px solid #e8eff6;">              <span style="font-weight:700;font-size:0.95rem;color:#1b3d7a;">Notifications</span>              <button onclick="markAllNotifRead()" style="background:none;border:none;font-family:Poppins,sans-serif;font-size:0.72rem;font-weight:600;color:#2563eb;cursor:pointer;padding:4px 8px;border-radius:6px;transition:background 0.15s;" onmouseover="this.style.background='#e8f0fe'" onmouseout="this.style.background='none'">Mark all read</button>            </div>            <div id="notifList" style="max-height:350px;overflow-y:auto;padding:8px 0;"></div>          </div>        </div>      </div>    </header>    <!-- ── Content ───────────────────────────── -->    <div class="dash-content">      <!-- DASHBOARD PAGE -->      <div id="page-dashboard">        <!-- Welcome banner -->        <div class="dash-welcome">          <div class="dash-welcome__text">            <h2 id="welcomeTitle">Welcome, Providers!</h2>            <p id="welcomeDesc">Connect with clients who value your expertise and grow your freelancing career with confidence.</p>          </div>        </div>        <!-- Dashboard Feed -->        <div id="dashFeed" style="margin-top:20px;">          <div id="feedLoading" style="text-align:center;padding:40px 20px;color:#7a8fa0;font-family:Poppins,sans-serif;font-size:0.88rem;">            Loading your feed...          </div>          <div id="feedContent" style="display:none;"></div>        </div>      </div><!-- /#page-dashboard -->      <!-- JOBS PAGE (Client = Post Jobs, Freelancer = Browse Jobs) -->      <div id="page-services" style="display:none;">        <!-- Job toolbar (role-dependent) -->        <div class="services-toolbar">          <!-- Client: Post Job -->          <button id="postJobBtn" class="services-add-btn" style="display:none;" onclick="openPostJob()">            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="16"/><line x1="8" y1="12" x2="16" y2="12"/></svg>            Post Job          </button>          <!-- Freelancer: Browse Jobs Search Bar -->          <div id="browseJobsSearchBar" style="display:none;flex:1;min-width:200px;max-width:420px;position:relative;">            <input type="text" id="browseKeywordToolbar" placeholder="Search jobs by keyword..."               style="width:100%;padding:10px 14px 10px 36px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;outline:none;transition:border-color 0.2s;"              onfocus="this.style.borderColor='var(--cyan)'" onblur="this.style.borderColor='#d0dbe6'"              oninput="document.getElementById('browseKeyword').value=this.value;applyBrowseFilters()" />            <svg style="position:absolute;left:12px;top:50%;transform:translateY(-50%);opacity:0.4;" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>          </div>        </div>        <!-- Header bar -->        <div class="services-header-bar" id="jobsHeaderBar">My Jobs</div>        <!-- Freelancer Browse Search/Filter Bar -->        <div id="browseSearchBar" style="display:none;padding:12px 0;">          <div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center;">            <div style="flex:1;min-width:200px;position:relative;">              <input type="text" id="browseKeyword" placeholder="Search jobs by keyword..."                 style="width:100%;padding:10px 14px 10px 36px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;outline:none;transition:border-color 0.2s;"                onfocus="this.style.borderColor='var(--cyan)'" onblur="this.style.borderColor='#d0dbe6'"                oninput="applyBrowseFilters()" />              <svg style="position:absolute;left:12px;top:50%;transform:translateY(-50%);opacity:0.4;" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>            </div>            <select id="browseCategoryFilter"               style="padding:10px 14px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;background:#fff;cursor:pointer;min-width:180px;outline:none;"              onchange="applyBrowseFilters()">              <option value="">All Categories</option>            </select>            <select id="browseSubcategoryFilter"               style="padding:10px 14px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;background:#fff;cursor:pointer;min-width:180px;outline:none;display:none;"              onchange="applyBrowseFilters()">              <option value="">All Subcategories</option>            </select>            <button onclick="clearBrowseFilters()"               style="padding:10px 16px;border:1.5px solid #ef4444;background:#fff;color:#ef4444;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.78rem;font-weight:600;cursor:pointer;transition:all 0.2s;white-space:nowrap;"              onmouseover="this.style.background='#ef4444';this.style.color='#fff'"               onmouseout="this.style.background='#fff';this.style.color='#ef4444'">✕ Clear</button>          </div>        </div>        <!-- Filter tabs -->        <div class="services-tabs" id="jobFilterTabs">          <button class="services-tab active" onclick="filterJobs('all', this)">All</button>          <button class="services-tab" onclick="filterJobs('open', this)">Open</button>          <button class="services-tab" onclick="filterJobs('in-progress', this)">In Progress</button>          <button class="services-tab" onclick="filterJobs('completed', this)">Completed</button>        </div>        <!-- Jobs grid -->        <div class="services-grid" id="jobsGrid">          <!-- Populated by JS -->        </div>      </div><!-- /#page-services -->      <!-- PROFILE PAGE — role-aware, fully editable -->      <div id="page-profile" style="display:none;">        <div class="profile-page" id="profilePage">          <!-- ── Hero card ── -->          <div class="profile-hero">            <div class="profile-hero__banner"></div>            <div class="profile-hero__body">              <div class="profile-hero__avatar-wrap">                <div class="profile-hero__avatar" id="profileAvatarDisplay">👤</div>                <div class="profile-hero__avatar-edit" onclick="document.getElementById('avatarInput').click()" title="Change photo">                  <svg viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>                </div>                <input type="file" id="avatarInput" accept="image/*" style="display:none" onchange="handleAvatarChange(event)" />              </div>              <div class="profile-hero__info">                <div>                  <div class="profile-hero__name" id="profileNameDisplay">Jane Doe</div>                  <div class="profile-hero__title" id="profileTitleDisplay">Software Engineer &amp; UI/UX Designer</div>                  <div class="profile-hero__since">Member since 2023</div>                </div>                <div style="display:flex;align-items:flex-start;gap:24px;flex-wrap:wrap;">                  <div class="profile-hero__meta" id="profileMetaDisplay">                    English and Tagalog<br>Manila, Philippines                  </div>                  <button class="profile-edit-btn" id="editProfileBtn" onclick="toggleProfileEdit()">                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>                    Edit                  </button>                </div>              </div>            </div>            <div class="profile-hero__bio" id="profileBioDisplay">              I help startups and small businesses turn ideas into clean, user-friendly interfaces. With 5+ years of experience in product design, I specialize in mobile apps, dashboards, and landing pages.            </div>          </div>          <!-- ── Main 2-col grid ── -->          <div class="profile-grid">            <!-- LEFT: Skills & Tools -->            <div class="profile-panel" id="panelSkills">              <div class="profile-panel__header">                <span class="profile-panel__title">Skills and Tools</span>              </div>              <div class="profile-panel__body">                <div class="prof-field">                  <div class="prof-field__label">Primary skill</div>                  <input class="prof-field__value" id="pfSkill" value="UI/UX Design" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Frequently Used Tools</div>                  <input class="prof-field__value" id="pfTools" value="Figma, Adobe XD, Sketch" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Skill proficiency level</div>                  <input class="prof-field__value" id="pfLevel" value="Expert" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Years of experience</div>                  <input class="prof-field__value" id="pfYears" value="6 years" readonly />                </div>              </div>            </div>            <!-- RIGHT: Reviews -->            <div class="profile-panel">              <div class="profile-panel__header">                <span class="profile-panel__title">Reviews</span>                <div class="profile-panel__badge" id="profileAvgBadge">⭐ —</div>              </div>              <div class="profile-panel__body">                <div id="profileReviewsList">                  <div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:20px 0;font-family:Poppins,sans-serif;">No reviews yet. Reviews from other users will appear here.</div>                </div>              </div>            </div>            <!-- LEFT BOTTOM: Rates + Portfolio + Activity -->            <div style="display:flex;flex-direction:column;gap:20px;">              <!-- Rates and Payments -->              <div class="profile-panel">                <div class="profile-panel__header">                  <span class="profile-panel__title">Rates and Payments</span>                </div>                <div class="profile-panel__body">                  <div class="rates-row">                    <span class="rates-label">Hourly Rate</span>                    <span style="font-size:.78rem;color:#8a9baa;">₱</span>                    <input class="rates-pill" id="pfHourly" value="3,800" readonly style="width:80px;text-align:center;" />                    <span style="font-size:.78rem;color:#8a9baa;">/hr</span>                  </div>                  <div class="rates-row">                    <span class="rates-label">Fixed Project Rate</span>                    <span style="font-size:.78rem;color:#8a9baa;">₱</span>                    <input class="rates-pill" id="pfFixedMin" value="46,000" readonly style="width:88px;text-align:center;" />                    <span class="rates-sep">–</span>                    <input class="rates-pill" id="pfFixedMax" value="464,000" readonly style="width:96px;text-align:center;" />                  </div>                  <div class="rates-row">                    <span class="rates-label">Preferred Payment Method</span>                    <input class="rates-pill" id="pfPayment" value="Bank Transfer/E-Wallet" readonly style="width:auto;flex:1;" />                  </div>                </div>              </div>              <!-- Portfolio -->              <div class="profile-panel">                <div class="profile-panel__header">                  <span class="profile-panel__title">Portfolio</span>                </div>                <div class="profile-panel__body">                  <button class="portfolio-btn" onclick="togglePortfolioView()">                    <span>View Full Portfolio</span>                    <div class="portfolio-btn__icon">                      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">                        <polyline points="15 3 21 3 21 9"/><polyline points="9 21 3 21 3 15"/>                        <line x1="21" y1="3" x2="14" y2="10"/><line x1="3" y1="21" x2="10" y2="14"/>                      </svg>                    </div>                  </button>                  <!-- No portfolio yet -->                  <div id="portfolioEmpty" style="display:block;">                    <div class="portfolio-upload" onclick="document.getElementById('portfolioPdfInput').click()">                      <div class="portfolio-upload__icon">📄</div>                      <div class="portfolio-upload__text">                        No portfolio uploaded yet.<br>                        <span class="portfolio-upload__link">Click to upload a PDF</span> of your professional portfolio.                      </div>                    </div>                    <input type="file" id="portfolioPdfInput" accept="application/pdf" style="display:none" onchange="handlePortfolioUpload(event)" />                  </div>                  <!-- Portfolio uploaded -->                  <div id="portfolioFile" style="display:none;">                    <div class="portfolio-file-display">                      <div class="portfolio-file-icon">📄</div>                      <div class="portfolio-file-name" id="portfolioFileName">portfolio.pdf</div>                      <button class="portfolio-file-remove" onclick="removePortfolio()">✕ Remove</button>                    </div>                    <div style="display:flex;gap:10px;margin-top:10px;">                      <button onclick="viewPortfolioPdf()" style="flex:1;background:var(--cyan);color:#fff;border:none;border-radius:999px;padding:8px 12px;font-family:Poppins,sans-serif;font-size:.75rem;font-weight:700;cursor:pointer;">View PDF</button>                      <button onclick="document.getElementById('portfolioPdfInput').click()" style="flex:1;background:#f0f4f8;color:#4a5a6a;border:none;border-radius:999px;padding:8px 12px;font-family:Poppins,sans-serif;font-size:.75rem;font-weight:700;cursor:pointer;">Replace</button>                    </div>                  </div>                </div>              </div>              <!-- Activity -->              <div class="profile-panel">                <div class="profile-panel__header">                  <span class="profile-panel__title">Activity</span>                </div>                <div class="profile-panel__body">                  <div class="activity-boxes">                    <div class="activity-box"><div class="activity-box__num" id="actPosted">0</div><div class="activity-box__lbl" id="actPostedLabel">Posted</div></div>                    <div class="activity-box"><div class="activity-box__num" id="actCompleted">0</div><div class="activity-box__lbl">Completed</div></div>                    <div class="activity-box"><div class="activity-box__num" id="actOngoing">0</div><div class="activity-box__lbl">In Progress</div></div>                  </div>                </div>              </div>            </div><!-- /left bottom stack -->            <!-- RIGHT BOTTOM: Education & Credentials -->            <div class="profile-panel">              <div class="profile-panel__header">                <span class="profile-panel__title">Education and Credentials</span>              </div>              <div class="profile-panel__body">                <div class="prof-field">                  <div class="prof-field__label">Degree / Field of Study</div>                  <input class="prof-field__value" id="pfDegree" value="B.S. in Computer Science" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Institution</div>                  <input class="prof-field__value" id="pfInstitution" value="University of the Philippines" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Year of Graduation</div>                  <input class="prof-field__value" id="pfGradYear" value="2019" readonly />                </div>                <div class="prof-field" style="margin-top:18px;">                  <div class="prof-field__label">Certifications</div>                  <input class="prof-field__value" id="pfCerts" value="Google UX Design Certificate" readonly />                </div>              </div>            </div>          </div><!-- /.profile-grid -->        </div><!-- /.profile-page -->      </div><!-- /#page-profile -->            <!-- MESSAGES PAGE (chat demo) -->      <div id="page-messages" style="display:none;">        <div style="display:flex;gap:0;height:calc(100vh - 120px);max-height:calc(100vh - 120px);background:#fff;border-radius:16px;box-shadow:0 2px 16px rgba(0,0,0,0.07);overflow:hidden;">                    <!-- Inbox Sidebar -->          <div style="width:260px;min-width:220px;border-right:1.5px solid #e8eff6;display:flex;flex-direction:column;background:#f8fafc;">            <div style="padding:20px 18px 14px;font-weight:700;font-size:1.05rem;color:#1b3d7a;border-bottom:1.5px solid #e8eff6;font-family:Poppins,sans-serif;">              💬 Inbox            </div>            <div id="inboxList" style="flex:1;overflow-y:auto;padding:10px 10px;display:flex;flex-direction:column;gap:4px;"></div>          </div>          <!-- Chat Area -->          <div style="flex:1;display:flex;flex-direction:column;min-width:0;">                        <!-- Chat Header -->            <div style="padding:16px 22px;border-bottom:1.5px solid #e8eff6;display:flex;align-items:center;gap:12px;background:#fff;">              <div id="chatAvatarHeader" style="width:36px;height:36px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.85rem;font-family:Poppins,sans-serif;flex-shrink:0;">                ?              </div>              <div style="flex:1;">                <div id="chatPartnerName" style="font-weight:700;font-size:0.95rem;color:#1a2a3a;font-family:Poppins,sans-serif;"></div>                <div id="chatPartnerStatus" style="font-size:0.72rem;color:#7a8fa0;font-family:Poppins,sans-serif;">Select a conversation</div>              </div>              <!-- View Profile Button -->              <button id="viewPartnerProfileBtn" onclick="togglePartnerProfile()" style="display:none;align-items:center;gap:6px;border:1.5px solid #1b3d7a;background:#fff;color:#1b3d7a;font-family:Poppins,sans-serif;font-weight:600;font-size:0.72rem;border-radius:999px;padding:6px 14px;cursor:pointer;transition:all 0.2s;white-space:nowrap;" onmouseover="this.style.background='#1b3d7a';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#1b3d7a'">                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>                View Profile              </button>            </div>            <!-- Messages Container -->            <div id="chatBox" style="flex:1;overflow-y:auto;padding:20px 22px;display:flex;flex-direction:column;gap:10px;background:#f4f7fb;">              <div style="text-align:center;color:#aab5c0;font-size:0.85rem;margin:auto;font-family:Poppins,sans-serif;">                👈 Select a user from the inbox to start chatting              </div>            </div>            <!-- Message Input -->            <form id="chatForm" onsubmit="return sendMessage(event)" style="padding:14px 18px;border-top:1.5px solid #e8eff6;display:flex;gap:10px;align-items:center;background:#fff;">              <input id="chatInput" type="text" placeholder="Type a message…" autocomplete="off"                 style="flex:1;padding:11px 16px;border-radius:999px;border:1.5px solid #dce4ef;font-size:0.88rem;font-family:Poppins,sans-serif;outline:none;background:#f8fafc;transition:border-color 0.2s;"                onfocus="this.style.borderColor='#1b3d7a'" onblur="this.style.borderColor='#dce4ef'" />              <button type="submit" style="background:linear-gradient(135deg,#1b3d7a,#2563eb);color:#fff;border:none;border-radius:50%;width:42px;height:42px;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0;box-shadow:0 2px 10px rgba(27,61,122,0.25);transition:transform 0.15s;">                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">                  <line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/>                </svg>              </button>            </form>          </div>          <!-- Partner Profile Sidebar (hidden by default) -->          <div id="partnerProfilePanel" style="width:0;overflow:hidden;border-left:0;background:#f8fafc;display:flex;flex-direction:column;transition:width 0.3s ease, border-left 0.3s ease, padding 0.3s ease;">            <div id="partnerProfileContent" style="width:280px;padding:0;display:flex;flex-direction:column;gap:0;overflow-y:auto;">                            <!-- Profile Header -->              <div style="padding:22px 20px 16px;border-bottom:1.5px solid #e8eff6;text-align:center;">                <div id="ppAvatar" style="width:64px;height:64px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:1.4rem;font-family:Poppins,sans-serif;margin:0 auto 10px;"></div>                <div id="ppName" style="font-weight:700;font-size:1rem;color:#1a2a3a;font-family:Poppins,sans-serif;"></div>                <div id="ppTitle" style="font-size:0.75rem;color:#7a8fa0;font-family:Poppins,sans-serif;margin-top:2px;"></div>                <button onclick="togglePartnerProfile()" style="margin-top:10px;background:none;border:1.5px solid #dce4ef;border-radius:999px;padding:5px 14px;font-family:Poppins,sans-serif;font-size:0.7rem;font-weight:600;color:#7a8fa0;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.borderColor='#ef4444';this.style.color='#ef4444'" onmouseout="this.style.borderColor='#dce4ef';this.style.color='#7a8fa0'">✕ Close</button>              </div>              <!-- Skills Info -->              <div style="padding:18px 20px;border-bottom:1.5px solid #e8eff6;">                <div style="font-weight:700;font-size:0.8rem;color:#1b3d7a;font-family:Poppins,sans-serif;margin-bottom:14px;text-transform:uppercase;letter-spacing:0.06em;">Skills & Expertise</div>                                <div style="margin-bottom:12px;">                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Primary Skill</div>                  <div id="ppSkill" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>                <div style="margin-bottom:12px;">                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Frequently Used Tools</div>                  <div id="ppTools" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>                <div style="margin-bottom:12px;">                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Skill Proficiency Level</div>                  <div id="ppLevel" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>                <div>                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Years of Experience</div>                  <div id="ppYears" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>              </div>              <!-- Rating Section -->              <div style="padding:18px 20px;border-bottom:1.5px solid #e8eff6;">                <div style="font-weight:700;font-size:0.8rem;color:#1b3d7a;font-family:Poppins,sans-serif;margin-bottom:6px;text-transform:uppercase;letter-spacing:0.06em;">Average Rating</div>                <div style="display:flex;align-items:center;gap:8px;margin-bottom:14px;">                  <div id="ppAvgRating" style="font-size:1.6rem;font-weight:800;color:#f5a623;font-family:Poppins,sans-serif;">—</div>                  <div id="ppAvgStars" style="font-size:1.1rem;color:#f5a623;letter-spacing:2px;">☆☆☆☆☆</div>                  <div id="ppRatingCount" style="font-size:0.7rem;color:#8a9baa;font-family:Poppins,sans-serif;">(0 reviews)</div>                </div>                <!-- Your Rating -->                <div style="background:#f7fbff;border-radius:12px;padding:14px 16px;">                  <div style="font-size:0.72rem;font-weight:600;color:#5a6a7a;margin-bottom:8px;font-family:Poppins,sans-serif;">Your Rating</div>                  <div id="ppStarPicker" style="display:flex;gap:4px;margin-bottom:10px;">                    <span class="star-pick" data-star="1" onclick="pickStar(1)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="2" onclick="pickStar(2)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="3" onclick="pickStar(3)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="4" onclick="pickStar(4)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="5" onclick="pickStar(5)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                  </div>                  <textarea id="ppReviewText" placeholder="Write a short review (optional)…" style="width:100%;border:1.5px solid #dce4ef;border-radius:8px;padding:8px 12px;font-family:Poppins,sans-serif;font-size:0.78rem;outline:none;resize:vertical;min-height:60px;transition:border-color 0.2s;" onfocus="this.style.borderColor='#1b3d7a'" onblur="this.style.borderColor='#dce4ef'"></textarea>                  <button onclick="submitReview()" style="margin-top:8px;width:100%;padding:9px;background:linear-gradient(135deg,#1b3d7a,#2563eb);color:#fff;border:none;border-radius:999px;font-family:Poppins,sans-serif;font-weight:700;font-size:0.75rem;letter-spacing:0.06em;text-transform:uppercase;cursor:pointer;box-shadow:0 2px 10px rgba(27,61,122,0.25);transition:opacity 0.2s;" onmouseover="this.style.opacity='0.88'" onmouseout="this.style.opacity='1'">Submit Review</button>                </div>              </div>              <!-- Reviews List -->              <div style="padding:18px 20px;">                <div style="font-weight:700;font-size:0.8rem;color:#1b3d7a;font-family:Poppins,sans-serif;margin-bottom:12px;text-transform:uppercase;letter-spacing:0.06em;">Reviews</div>                <div id="ppReviewsList" style="display:flex;flex-direction:column;gap:10px;">                  <div style="text-align:center;color:#aab5c0;font-size:0.78rem;padding:16px 0;font-family:Poppins,sans-serif;">No reviews yet</div>                </div>              </div>            </div>          </div>        </div>      </div>      <!-- SETTINGS PAGE (update email/password) -->      <div id="page-settings" style="display:none;">        <div class="dash-panel" style="max-width:600px;">          <div class="dash-panel__header"><span class="dash-panel__title">Settings</span></div>          <form id="settingsForm" onsubmit="return saveSettings(event)" style="display:flex;flex-direction:column;gap:18px;margin-top:18px;">            <label>              <span style="font-size:.95rem;color:#1a2a3a;">Email Address</span>              <input id="settingsEmail" type="email" required style="width:100%;padding:10px 14px;border-radius:8px;border:1.5px solid #e0e8f0;font-size:.95rem;" />            </label>            <label>              <span style="font-size:.95rem;color:#1a2a3a;">New Password</span>              <input id="settingsPassword" type="password" minlength="6" placeholder="Leave blank to keep current" style="width:100%;padding:10px 14px;border-radius:8px;border:1.5px solid #e0e8f0;font-size:.95rem;" />            </label>            <button type="submit" style="background:#1b3d7a;color:#fff;border:none;border-radius:8px;padding:10px 0;font-weight:700;cursor:pointer;">Save Changes</button>          </form>          <div style="margin-top:30px;padding-top:20px;border-top:1px solid #e0e8f0;">            <button onclick="logout()" style="width:100%;background:#f44336;color:#fff;border:none;border-radius:8px;padding:12px 0;font-weight:700;cursor:pointer;font-size:.95rem;">Sign Out</button>          </div>        </div>      </div>    </div><!-- /.dash-content -->  </div><!-- /.dash-main --></div><!-- /.dash-shell --><!-- ═══════════════════════════════════════════     ROLE SWITCH MODAL     ═══════════════════════════════════════════ --><div class="role-modal-overlay" id="roleModalOverlay">  <div class="role-modal">    <div class="role-modal__title">Switch Your Role</div>    <div class="role-modal__sub">Choose how you want to use GigHub right now.</div>    <div class="role-modal__options">      <div class="role-option" id="roleOptClient" onclick="selectRole('client')">        <div class="role-option__icon">🏢</div>        <div class="role-option__label">Client</div>        <div class="role-option__desc">Post jobs & hire talent</div>      </div>      <div class="role-option selected" id="roleOptFreelancer" onclick="selectRole('freelancer')">        <div class="role-option__icon">💼</div>        <div class="role-option__label">Freelancer</div>        <div class="role-option__desc">Offer services & earn</div>      </div>    </div>    <button class="role-modal__confirm" onclick="confirmRole()">Confirm Role</button>  </div></div><!-- EDIT JOB MODAL (Client) --><div class="role-modal-overlay" id="editJobOverlay">  <div class="role-modal" style="width:520px;max-height:90vh;overflow-y:auto;">    <div class="role-modal__title">Edit Job</div>    <div class="role-modal__sub">Update your job details</div>    <div style="display:flex;flex-direction:column;gap:14px;margin-bottom:20px;">      <input id="editJobTitle" class="auth-field__input" type="text" placeholder="Job title" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />            <textarea id="editJobDescription" placeholder="Job description" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;resize:vertical;min-height:100px;"></textarea>            <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">        <input id="editJobBudgetMin" type="number" placeholder="Min Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />        <input id="editJobBudgetMax" type="number" placeholder="Max Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />      </div>            <input id="editJobDuration" type="text" placeholder="Expected duration (e.g. 2 weeks)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />    </div>    <div style="display:flex;gap:12px;">      <button class="role-modal__confirm" onclick="saveEditJob()" style="flex:1;">Save Changes</button>      <button onclick="closeEditJob()" style="flex:1;border:2px solid #e0e8f0;background:#fff;border-radius:999px;padding:13px;font-family:Poppins,sans-serif;font-weight:700;font-size:.85rem;cursor:pointer;color:#7a8fa0;">Cancel</button>    </div>  </div></div><!-- POST JOB MODAL (Client) --><div class="role-modal-overlay" id="postJobOverlay">  <div class="role-modal" style="width:520px;max-height:90vh;overflow-y:auto;">    <div class="role-modal__title">Post a New Job</div>    <div class="role-modal__sub">Tell freelancers what you need done</div>    <div style="display:flex;flex-direction:column;gap:14px;margin-bottom:20px;">      <input id="jobTitle" class="auth-field__input" type="text" placeholder="Job title (e.g. Logo Design)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />            <select id="jobCategory" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;color:#4a5a6a;">        <option value="">Select Category</option>        <!-- Populated by JS -->      </select>            <select id="jobSubcategory" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;color:#4a5a6a;">        <option value="">Select Subcategory</option>        <!-- Populated by JS -->      </select>            <textarea id="jobDescription" placeholder="Job description - what do you need?" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;resize:vertical;min-height:100px;"></textarea>            <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">        <input id="jobBudgetMin" type="number" placeholder="Min Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />        <input id="jobBudgetMax" type="number" placeholder="Max Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />      </div>            <input id="jobDuration" type="text" placeholder="Expected duration (e.g. 2 weeks)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />            <input id="jobAttachment" type="text" placeholder="Attachment filename (optional)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />    </div>    <div style="display:flex;gap:12px;">      <button class="role-modal__confirm" onclick="submitJob()" style="flex:1;">Post Job</button>      <button onclick="closePostJob()" style="flex:1;border:2px solid #e0e8f0;background:#fff;border-radius:999px;padding:13px;font-family:Poppins,sans-serif;font-weight:700;font-size:.85rem;cursor:pointer;color:#7a8fa0;">Cancel</button>    </div>  </div></div><script>  /* ══════════════════════════════════════════     STATE     ══════════════════════════════════════════ */  const API_BASE = '/api';  let currentUser = null;  let currentRole = 'freelancer';  let pendingRole = 'freelancer';  let isEditingProfile = false;  let portfolioObjectUrl = null;  let account = null;  let isBrowsingAvailableJobs = false;  let jobs = [];  let categories = [];  /* Independent profiles per user/role with credentials */  let profiles = { freelancer: {}, client: {} };  /* ══════════════════════════════════════════     PAGE NAVIGATION     ══════════════════════════════════════════ */  function setPage(btn) {    const page = btn.dataset.page;    console.log('🔖 setPage called with:', page);    document.querySelectorAll('.dash-nav__item').forEach(el => el.classList.remove('active'));    btn.classList.add('active');    // Find current visible page and target page    const allPages = document.querySelectorAll('[id^="page-"]');    let outEl = null;    allPages.forEach(el => {      if (el.style.display !== 'none' && el.id !== 'page-' + page) outEl = el;    });    const inEl = document.getElementById('page-' + page);    // Use animated transition    if (typeof GigHubAnimations !== 'undefined') {      GigHubAnimations.transitionPages(outEl, inEl, () => {        // Hide all other pages (safety)        allPages.forEach(el => { if (el !== inEl) el.style.display = 'none'; });      }).then(() => {        afterPageSwitch(page);      });    } else {      // Fallback: no animation      allPages.forEach(el => el.style.display = 'none');      inEl.style.display = '';      afterPageSwitch(page);    }  }  function afterPageSwitch(page) {    if (page === 'dashboard') {      loadDashboardFeed();      if (typeof GigHubAnimations !== 'undefined') GigHubAnimations.animateWelcome();    }    if (page === 'services') {      console.log('📂 Switching to Services tab - will load and render jobs');      isBrowsingAvailableJobs = false;      activeJobFilter = 'all';      const searchBar = document.getElementById('browseSearchBar');      if (searchBar) searchBar.style.display = 'none';      loadJobsForRole().then(() => {        console.log('✅ loadJobsForRole completed, now rendering...');        renderJobs(activeJobFilter);        updateJobButtons();        document.querySelectorAll('.services-tab').forEach(t => t.classList.remove('active'));        document.querySelectorAll('.services-tab')[0]?.classList.add('active');        if (typeof GigHubAnimations !== 'undefined') {          GigHubAnimations.staggerChildren('#jobsGrid', '.service-card');        }      }).catch(err => {        console.error('❌ Error loading jobs:', err);        renderJobs(activeJobFilter);      });    }    if (page === 'profile')  renderProfile();    if (page === 'messages') {      loadInbox();      document.getElementById('chatBox').innerHTML = '';      document.getElementById('chatPartnerName').textContent = '';      chatPartner = '';    }    if (page === 'settings') {      document.getElementById('settingsEmail').value = account.email || '';      document.getElementById('settingsPassword').value = '';    }  }  /* ══════════════════════════════════════════     PAGE INITIALIZATION     ══════════════════════════════════════════ */  window.addEventListener('load', async () => {    const sessionUser = localStorage.getItem('gighub_user');    if (!sessionUser) {      window.location.href = 'login.html';      return;    }    currentUser = sessionUser;    // Load user data from server first    try {      const res = await fetch(`${API_BASE}/users/${encodeURIComponent(currentUser)}`);      const data = await res.json();      if (data.success && data.user) {        account = data.user;        currentRole = account.currentRole || 'freelancer';        pendingRole = currentRole;        // Load both role profiles from server — preserve both        if (account.roles?.freelancer) {          profiles.freelancer = { ...profiles.freelancer, ...account.roles.freelancer };        }        if (account.roles?.client) {          profiles.client = { ...profiles.client, ...account.roles.client };        }        // Populate settings fields        const settingsEmail = document.getElementById('settingsEmail');        if (settingsEmail) settingsEmail.value = account.email || '';      }    } catch (err) {      console.error('Error loading user data:', err);    }    try {      await loadJobsForRole();    } catch (err) {      console.error('Error loading jobs:', err);    }    renderProfile();    applyTheme(currentRole);    updateJobButtons();    populateCategoryDropdown();    loadDashboardFeed();  });  /* Update job buttons based on current role */  function updateJobButtons() {    const isFreelancer = currentRole === 'freelancer';    const postBtn = document.getElementById('postJobBtn');    const browseSearchBar = document.getElementById('browseJobsSearchBar');    const filterBar = document.getElementById('browseSearchBar');        if (!postBtn || !browseSearchBar) {      console.warn('Job buttons not found in DOM');      return;    }        if (isFreelancer) {      postBtn.style.display = 'none';      browseSearchBar.style.display = 'flex';      // Auto-enter browse mode      if (!isBrowsingAvailableJobs) {        isBrowsingAvailableJobs = true;        populateBrowseCategoryFilter();      }      if (filterBar) filterBar.style.display = '';    } else {      postBtn.style.display = 'inline-flex';      browseSearchBar.style.display = 'none';      postBtn.style.borderColor = '#f08020';      postBtn.style.color = '#f08020';      if (filterBar) filterBar.style.display = 'none';      isBrowsingAvailableJobs = false;    }  }  /* ══════════════════════════════════════════     ROLE MODAL     ══════════════════════════════════════════ */  function openRoleModal() {    pendingRole = currentRole;    highlightRole(pendingRole);    document.getElementById('roleModalOverlay').classList.add('active');  }  function selectRole(r) { pendingRole = r; highlightRole(r); }  function highlightRole(r) {    document.getElementById('roleOptClient').classList.toggle('selected',     r === 'client');    document.getElementById('roleOptFreelancer').classList.toggle('selected', r === 'freelancer');  }  function confirmRole() {    // Save current profile data BEFORE switching    if (isEditingProfile) {      exitProfileEdit();    }    currentRole = pendingRole;    document.getElementById('roleModalOverlay').classList.remove('active');    applyTheme(currentRole);    updateJobButtons();    saveSettings(null, { silent: true, persistPassword: false, overrideRole: currentRole });        // Reset filter and reload jobs for new role    activeJobFilter = 'all';    loadJobsForRole().then(() => {      document.querySelectorAll('.services-tab')[0]?.classList.add('active');    }).catch(err => {      console.error('Error loading jobs after role change:', err);    });        // Reload feed for new role    loadDashboardFeed();    // Re-render profile for the NEW role (using its own saved data)    if (document.getElementById('page-profile').style.display !== 'none') renderProfile();  }  document.getElementById('roleModalOverlay').addEventListener('click', function(e) {    if (e.target === this) this.classList.remove('active');  });    document.getElementById('editJobOverlay').addEventListener('click', function(e) {    if (e.target === this) this.classList.remove('active');  });    document.getElementById('postJobOverlay').addEventListener('click', function(e) {    if (e.target === this) this.classList.remove('active');  });  /* ══════════════════════════════════════════     THEME & DASHBOARD CONTENT     ══════════════════════════════════════════ */  function applyTheme(role) {    const shell = document.getElementById('dashShell');    const isFL = role === 'freelancer';    shell.setAttribute('data-role', role);    const avatarSvg = document.querySelector('.dash-topbar__avatar svg');    if (avatarSvg) avatarSvg.setAttribute('stroke', isFL ? '#1b3d7a' : '#c06010');    document.getElementById('welcomeTitle').textContent = isFL ? 'Welcome, Providers!' : 'Welcome, Seekers!';    document.getElementById('welcomeDesc').textContent  = isFL      ? 'Connect with clients who value your expertise and grow your freelancing career with confidence.'      : 'Work with skilled professionals who can bring your ideas to life — efficiently and reliably';  }  /* ══════════════════════════════════════════     DASHBOARD FEED     ══════════════════════════════════════════ */  async function loadDashboardFeed() {    const loading = document.getElementById('feedLoading');    const content = document.getElementById('feedContent');    if (!loading || !content) return;    loading.style.display = '';    content.style.display = 'none';    try {      if (currentRole === 'freelancer') {        await renderFreelancerFeed(content);      } else {        await renderClientFeed(content);      }      loading.style.display = 'none';      content.style.display = '';    } catch (err) {      console.error('Feed load error:', err);      loading.textContent = 'Failed to load feed.';    }  }  async function renderFreelancerFeed(container) {    const res = await fetch(`${API_BASE}/feed/freelancer/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) throw new Error(data.message);    const feedJobs = data.jobs || [];    const feedCats = data.categories || [];    function catName(id) {      const c = feedCats.find(cat => cat.id === id);      return c ? `${c.icon} ${c.name}` : id;    }    if (feedJobs.length === 0) {      container.innerHTML = `        <div style="text-align:center;padding:50px 20px;">          <div style="font-size:2.5rem;margin-bottom:12px;">🔍</div>          <div style="font-size:1rem;font-weight:600;color:#1a2a3a;margin-bottom:6px;font-family:Poppins,sans-serif;">No jobs available right now</div>          <div style="font-size:0.82rem;color:#7a8fa0;font-family:Poppins,sans-serif;">Check back later for new opportunities!</div>        </div>`;      return;    }    container.innerHTML = `      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px;">        <h3 style="font-family:Poppins,sans-serif;font-size:1.05rem;font-weight:700;color:#1b3d7a;margin:0;">💼 Jobs For You</h3>        <button onclick="document.querySelector('[data-page=services]').click()" style="background:none;border:none;font-family:Poppins,sans-serif;font-size:0.78rem;font-weight:600;color:#2563eb;cursor:pointer;">View All →</button>      </div>      <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:16px;">        ${feedJobs.map(job => {          const budgetText = (job.budgetMin || job.budgetMax)            ? `₱${job.budgetMin || 0} – ₱${job.budgetMax || 0}` : 'Budget not set';          const postedDate = job.postedAt ? new Date(job.postedAt).toLocaleDateString() : '';          return `            <div style="background:#fff;border-radius:14px;border:1.5px solid #e8eff6;padding:20px;font-family:Poppins,sans-serif;transition:all 0.2s;cursor:pointer;"                  onmouseover="this.style.borderColor='var(--cyan)';this.style.boxShadow='0 4px 20px rgba(0,200,240,0.12)'"                  onmouseout="this.style.borderColor='#e8eff6';this.style.boxShadow='none'">              <div style="display:flex;align-items:center;gap:10px;margin-bottom:10px;">                <span style="font-size:1.5rem;">${(catName(job.category).split(' ')[0]) || '💼'}</span>                <span class="dash-badge dash-badge--green" style="font-size:0.68rem;">Open</span>              </div>              <div style="font-size:0.92rem;font-weight:700;color:#1a2a3a;margin-bottom:4px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${job.title}</div>              <div style="font-size:0.75rem;color:#7a8fa0;margin-bottom:8px;">${catName(job.category)}${job.subcategory ? ' › ' + job.subcategory : ''}</div>              <div style="font-size:0.78rem;color:#5a6a7a;margin-bottom:10px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${job.description}</div>              <div style="display:flex;align-items:center;justify-content:space-between;">                <span style="font-size:0.82rem;font-weight:700;color:#1b3d7a;">${budgetText}</span>                <span style="font-size:0.7rem;color:#aab5c0;">${postedDate}</span>              </div>              <div style="font-size:0.72rem;color:#8a9baa;margin-top:6px;">Posted by: <strong>${job.postedBy}</strong></div>              ${job.duration ? `<div style="font-size:0.7rem;color:#8a9baa;margin-top:2px;">⏱ ${job.duration}</div>` : ''}              <button onclick="event.stopPropagation();feedAcceptJob('${job.id}')" style="width:100%;margin-top:12px;padding:8px;font-size:0.78rem;font-weight:700;font-family:Poppins,sans-serif;border:none;background:var(--cyan);color:#fff;border-radius:8px;cursor:pointer;box-shadow:0 2px 10px rgba(0,200,240,0.3);transition:all 0.2s;" onmouseover="this.style.opacity='0.88'" onmouseout="this.style.opacity='1'">✋ Accept Job</button>            </div>`;        }).join('')}      </div>`;  }  async function renderClientFeed(container) {    const res = await fetch(`${API_BASE}/feed/client/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) throw new Error(data.message);    const freelancers = data.freelancers || [];    if (freelancers.length === 0) {      container.innerHTML = `        <div style="text-align:center;padding:50px 20px;">          <div style="font-size:2.5rem;margin-bottom:12px;">👤</div>          <div style="font-size:1rem;font-weight:600;color:#1a2a3a;margin-bottom:6px;font-family:Poppins,sans-serif;">No freelancers to show yet</div>          <div style="font-size:0.82rem;color:#7a8fa0;font-family:Poppins,sans-serif;">Post a job to discover matching freelancers, or check back later!</div>        </div>`;      return;    }    container.innerHTML = `      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px;">        <h3 style="font-family:Poppins,sans-serif;font-size:1.05rem;font-weight:700;color:#c06010;margin:0;">🎯 Freelancers For You</h3>      </div>      <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:16px;">        ${freelancers.map(fl => {          const avatar = fl.avatarSrc            ? `<img src="${fl.avatarSrc}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`            : `<span style="font-size:1.3rem;">👤</span>`;          const skills = [fl.skill, fl.tools].filter(Boolean).join(' • ');          return `            <div style="background:#fff;border-radius:14px;border:1.5px solid #e8eff6;padding:20px;font-family:Poppins,sans-serif;transition:all 0.2s;cursor:pointer;"                 onmouseover="this.style.borderColor='#f08020';this.style.boxShadow='0 4px 20px rgba(240,128,32,0.12)'"                 onmouseout="this.style.borderColor='#e8eff6';this.style.boxShadow='none'"                 onclick="feedViewProfile('${fl.username}')">              <div style="display:flex;align-items:center;gap:12px;margin-bottom:12px;">                <div style="width:48px;height:48px;border-radius:50%;background:linear-gradient(135deg,#fde8d0,#fbd5a8);display:flex;align-items:center;justify-content:center;overflow:hidden;flex-shrink:0;">                  ${avatar}                </div>                <div style="min-width:0;">                  <div style="font-size:0.9rem;font-weight:700;color:#1a2a3a;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${fl.name}</div>                  <div style="font-size:0.75rem;color:#7a8fa0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${fl.title}</div>                </div>              </div>              ${fl.bio ? `<div style="font-size:0.78rem;color:#5a6a7a;margin-bottom:10px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${fl.bio}</div>` : ''}              ${skills ? `<div style="font-size:0.72rem;color:#1b3d7a;margin-bottom:6px;">🛠 ${skills}</div>` : ''}              <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;">                ${fl.level ? `<span style="font-size:0.7rem;background:#e8f4fd;color:#1b3d7a;padding:3px 8px;border-radius:6px;font-weight:600;">${fl.level}</span>` : ''}                ${fl.years ? `<span style="font-size:0.7rem;color:#8a9baa;">${fl.years} exp</span>` : ''}                ${fl.hourly ? `<span style="font-size:0.7rem;font-weight:700;color:#16a34a;">₱${fl.hourly}/hr</span>` : ''}                ${fl.location ? `<span style="font-size:0.7rem;color:#8a9baa;">📍 ${fl.location}</span>` : ''}              </div>              <div style="display:flex;gap:8px;margin-top:12px;">                <button onclick="event.stopPropagation();feedMessageUser('${fl.username}')" style="flex:1;padding:8px;font-size:0.76rem;font-weight:600;font-family:Poppins,sans-serif;border:1.5px solid #f08020;background:#fff;color:#f08020;border-radius:8px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.background='#f08020';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#f08020'">💬 Message</button>              </div>            </div>`;        }).join('')}      </div>`;  }  async function feedAcceptJob(jobId) {    if (!confirm('Accept this job?')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'PATCH',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ status: 'in-progress', acceptedBy: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed');      showToast('Job accepted!', 'success');      loadDashboardFeed();    } catch (err) {      showToast('Error: ' + err.message, 'error');    }  }  function feedMessageUser(username) {    // Switch to messages page and open chat with this user    chatPartner = username;    document.querySelector('[data-page=messages]').click();    setTimeout(() => openChat(username), 300);  }  function feedViewProfile(username) {    // Open a lightweight profile preview via message (simplest approach)    feedMessageUser(username);  }  /* ══════════════════════════════════════════     HELPER FUNCTIONS     ══════════════════════════════════════════ */  /* Show a toast notification */  function showToast(message, type) {    let toast = document.getElementById('gighubToast');    if (!toast) {      toast = document.createElement('div');      toast.id = 'gighubToast';      toast.style.cssText = 'position:fixed;top:24px;right:24px;z-index:99999;padding:14px 28px;border-radius:12px;font-family:Poppins,sans-serif;font-size:0.88rem;font-weight:600;color:#fff;box-shadow:0 4px 24px rgba(0,0,0,0.18);transform:translateY(-20px);opacity:0;transition:all 0.35s ease;pointer-events:none;max-width:400px;';      document.body.appendChild(toast);    }    toast.textContent = message;    if (type === 'success') toast.style.background = 'linear-gradient(135deg,#16a34a,#22c55e)';    else if (type === 'warning') toast.style.background = 'linear-gradient(135deg,#ea580c,#f59e0b)';    else toast.style.background = 'linear-gradient(135deg,#ef4444,#f87171)';    toast.style.opacity = '1';    toast.style.transform = 'translateY(0)';    clearTimeout(toast._timer);    toast._timer = setTimeout(() => {      toast.style.opacity = '0';      toast.style.transform = 'translateY(-20px)';    }, 3000);  }  /* Replace a display element with an editable input */  function replaceWithInput(displayId, editId, value, fontSize, fontWeight, isTextarea) {    const display = document.getElementById(displayId);    if (!display) return;    display.style.display = 'none';    const wrap = document.createElement('div');    wrap.id = editId + '-wrap';    let el;    if (isTextarea) {      el = document.createElement('textarea');      el.rows = 3;      el.style.resize = 'vertical';    } else {      el = document.createElement('input');      el.type = 'text';    }    el.id = editId;    el.value = value || '';    el.style.cssText = `width:100%;font-size:${fontSize};font-weight:${fontWeight};font-family:Poppins,sans-serif;border:1.5px solid #1b3d7a;border-radius:8px;padding:8px 12px;outline:none;background:#f7fbff;box-shadow:0 0 0 2px rgba(27,61,122,0.08);box-sizing:border-box;`;    wrap.appendChild(el);    display.parentNode.insertBefore(wrap, display.nextSibling);  }  /* Get the value from an edit input, falling back to the display element */  function getEditVal(editId, displayId) {    const el = document.getElementById(editId);    if (el) return el.value;    const display = document.getElementById(displayId);    return display ? display.textContent : '';  }  /* Star picker click handler */  function pickStar(n) {    selectedStarRating = n;    updateStarPicker(n);  }  /* ══════════════════════════════════════════     PROFILE — render & edit (role-independent)     ══════════════════════════════════════════ */  function renderProfile() {    const p = profiles[currentRole];    exitProfileEdit(); // always start in view mode    /* Hero */    document.getElementById('profileNameDisplay').textContent  = p.name;    document.getElementById('profileTitleDisplay').textContent = p.title;    document.getElementById('profileBioDisplay').textContent   = p.bio;    document.getElementById('profileMetaDisplay').innerHTML    = p.languages + '<br>' + p.location;    /* Avatar */    const av = document.getElementById('profileAvatarDisplay');    if (p.avatarSrc) {      av.innerHTML = `<img src="${p.avatarSrc}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`;    } else {      av.innerHTML = '👤';    }    /* Skills */    document.getElementById('pfSkill').value       = p.skill;    document.getElementById('pfTools').value       = p.tools;    document.getElementById('pfLevel').value       = p.level;    document.getElementById('pfYears').value       = p.years;    /* Rates */    document.getElementById('pfHourly').value      = p.hourly;    document.getElementById('pfFixedMin').value    = p.fixedMin;    document.getElementById('pfFixedMax').value    = p.fixedMax;    document.getElementById('pfPayment').value     = p.payment;    /* Education */    document.getElementById('pfDegree').value      = p.degree;    document.getElementById('pfInstitution').value = p.institution;    document.getElementById('pfGradYear').value    = p.gradYear;    document.getElementById('pfCerts').value       = p.certs;    /* Portfolio */    updatePortfolioUI(p);    /* Load real reviews */    loadProfileReviews();    /* Load real activity counters */    loadActivityCounters();  }  async function loadProfileReviews() {    try {      const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(currentUser)}`);      if (!res.ok) {        console.error('Reviews endpoint returned', res.status);        return;      }      const text = await res.text();      let data;      try { data = JSON.parse(text); } catch (e) {        console.error('Failed to parse reviews response:', text);        return;      }      const reviews = data.success ? (data.reviews || []) : [];      const badge = document.getElementById('profileAvgBadge');      const listEl = document.getElementById('profileReviewsList');      if (reviews.length === 0) {        badge.textContent = '⭐ —';        listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:20px 0;font-family:Poppins,sans-serif;">No reviews yet. Reviews from other users will appear here.</div>';        return;      }      const avg = reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length;      badge.textContent = `⭐ ${avg.toFixed(1)}`;      listEl.innerHTML = reviews.map(r => {        const initial = r.from.charAt(0).toUpperCase();        const stars = renderStarString(r.rating);        return `          <div class="review-item">            <div class="review-avatar">${initial}</div>            <div>              <div style="font-size:0.78rem;font-weight:700;color:#1a2a3a;margin-bottom:2px;">${r.from}</div>              <div class="review-text">${r.text || 'No comment.'}</div>              <div class="review-stars">${stars}</div>            </div>          </div>`;      }).join('');    } catch (err) {      console.error('Error loading profile reviews:', err);    }  }  /* Toggle edit / save */  function toggleProfileEdit() {    if (isEditingProfile) {      saveProfileEdit();    } else {      enterProfileEdit();    }  }  function enterProfileEdit() {    isEditingProfile = true;    const btn = document.getElementById('editProfileBtn');    btn.innerHTML = `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg> Save`;    /* Make all profile fields editable + add visual edit indicator */    const fields = ['pfSkill','pfTools','pfLevel','pfYears','pfHourly','pfFixedMin','pfFixedMax','pfPayment','pfDegree','pfInstitution','pfGradYear','pfCerts'];    fields.forEach(id => {      const el = document.getElementById(id);      if (el) {        el.removeAttribute('readonly');        el.style.background = '#f7fbff';        el.style.border = '1.5px solid #1b3d7a';        el.style.boxShadow = '0 0 0 2px rgba(27,61,122,0.08)';        el.style.cursor = 'text';        if (!el.placeholder || el.placeholder === '') el.placeholder = 'Click to edit…';      }    });    /* Swap hero display elements to inputs */    replaceWithInput('profileNameDisplay',  'pf-edit-name',  profiles[currentRole].name,      '1.1rem','700');    replaceWithInput('profileTitleDisplay', 'pf-edit-title', profiles[currentRole].title,     '0.8rem','400');    replaceWithInput('profileBioDisplay',   'pf-edit-bio',   profiles[currentRole].bio,       '0.84rem','400', true);    replaceWithInput('profileMetaDisplay',  'pf-edit-meta',  profiles[currentRole].languages + '\n' + profiles[currentRole].location, '0.8rem','400', true);    /* Show edit mode banner */    let banner = document.getElementById('editModeBanner');    if (!banner) {      banner = document.createElement('div');      banner.id = 'editModeBanner';      banner.style.cssText = 'background:linear-gradient(135deg,#1b3d7a,#2563eb);color:#fff;padding:10px 20px;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;font-weight:600;text-align:center;margin-bottom:16px;display:flex;align-items:center;justify-content:center;gap:8px;';      banner.innerHTML = '✏️ <span>Edit Mode — All highlighted fields are editable. Click Save when done.</span>';      const profilePage = document.getElementById('profilePage');      if (profilePage) profilePage.insertBefore(banner, profilePage.firstChild);    }    banner.style.display = 'flex';  }  function saveProfileEdit() {    const p = profiles[currentRole];    /* Hero fields */    const name      = getEditVal('pf-edit-name',  'profileNameDisplay').trim();    const title     = getEditVal('pf-edit-title', 'profileTitleDisplay').trim();    const bio       = getEditVal('pf-edit-bio',   'profileBioDisplay').trim();    const metaRaw   = getEditVal('pf-edit-meta', 'profileMetaDisplay').trim();    const metaLines = metaRaw.split('\n');    const languages = metaLines[0]?.trim() || '';    const location  = metaLines[1]?.trim() || '';    /* Fields */    const skill       = document.getElementById('pfSkill').value.trim();    const tools       = document.getElementById('pfTools').value.trim();    const level       = document.getElementById('pfLevel').value.trim();    const years       = document.getElementById('pfYears').value.trim();    const hourly      = document.getElementById('pfHourly').value.trim();    const fixedMin    = document.getElementById('pfFixedMin').value.trim();    const fixedMax    = document.getElementById('pfFixedMax').value.trim();    const payment     = document.getElementById('pfPayment').value.trim();    const degree      = document.getElementById('pfDegree').value.trim();    const institution = document.getElementById('pfInstitution').value.trim();    const gradYear    = document.getElementById('pfGradYear').value.trim();    const certs       = document.getElementById('pfCerts').value.trim();    /* Only validate the essentials — allow partial saves */    if (!name) {      alert('Name is required.');      return;    }    /* Save all data */    p.name       = name;    p.title      = title || '';    p.bio        = bio || '';    p.languages  = languages || '';    p.location   = location || '';    p.skill      = skill || '';    p.tools      = tools || '';    p.level      = level || '';    p.years      = years || '';    p.hourly     = hourly || '';    p.fixedMin   = fixedMin || '';    p.fixedMax   = fixedMax || '';    p.payment    = payment || '';    p.degree     = degree || '';    p.institution = institution || '';    p.gradYear   = gradYear || '';    p.certs      = certs || '';    /* Sync to database (fire and forget — no await needed) */    syncProfileToDatabase().then(saved => {      if (saved) {        showToast('✅ Profile saved successfully!', 'success');      } else {        showToast('⚠️ Profile saved locally but failed to sync to server.', 'warning');      }    });    exitProfileEdit();    renderProfile();  }  function exitProfileEdit() {    isEditingProfile = false;    const btn = document.getElementById('editProfileBtn');    if (btn) btn.innerHTML = `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg> Edit`;    const fields = ['pfSkill','pfTools','pfLevel','pfYears','pfHourly','pfFixedMin','pfFixedMax','pfPayment','pfDegree','pfInstitution','pfGradYear','pfCerts'];    fields.forEach(id => {      const el = document.getElementById(id);      if (el) {        el.setAttribute('readonly', '');        el.style.background = '';        el.style.border = '';        el.style.boxShadow = '';        el.style.cursor = '';      }    });    /* Remove edit wrappers */    ['pf-edit-name-wrap','pf-edit-title-wrap','pf-edit-bio-wrap','pf-edit-meta-wrap'].forEach(wid => {      const wrap = document.getElementById(wid);      if (wrap) wrap.remove();    });    /* Restore display elements */    ['profileNameDisplay','profileTitleDisplay','profileBioDisplay','profileMetaDisplay'].forEach(did => {      const el = document.getElementById(did);      if (el) el.style.display = '';    });    /* Hide edit banner */    const banner = document.getElementById('editModeBanner');    if (banner) banner.style.display = 'none';  }  /* ── Avatar upload ───────────────────────── */  function handleAvatarChange(event) {    const file = event.target.files[0];    if (!file) return;    const reader = new FileReader();    reader.onload = e => {      profiles[currentRole].avatarSrc = e.target.result;      const av = document.getElementById('profileAvatarDisplay');      av.innerHTML = `<img src="${e.target.result}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`;    };    reader.readAsDataURL(file);  }  /* ══════════════════════════════════════════     PORTFOLIO PDF     ══════════════════════════════════════════ */  function handlePortfolioUpload(event) {    const file = event.target.files[0];    if (!file || file.type !== 'application/pdf') return;    if (portfolioObjectUrl) URL.revokeObjectURL(portfolioObjectUrl);    portfolioObjectUrl = URL.createObjectURL(file);    profiles[currentRole].portfolioFile     = portfolioObjectUrl;    profiles[currentRole].portfolioFileName = file.name;    updatePortfolioUI(profiles[currentRole]);  }  function updatePortfolioUI(p) {    const empty = document.getElementById('portfolioEmpty');    const fileEl = document.getElementById('portfolioFile');    if (p.portfolioFile) {      empty.style.display  = 'none';      fileEl.style.display = '';      document.getElementById('portfolioFileName').textContent = p.portfolioFileName;    } else {      empty.style.display  = '';      fileEl.style.display = 'none';    }  }  function removePortfolio() {    if (portfolioObjectUrl) { URL.revokeObjectURL(portfolioObjectUrl); portfolioObjectUrl = null; }    profiles[currentRole].portfolioFile     = null;    profiles[currentRole].portfolioFileName = null;    document.getElementById('portfolioPdfInput').value = '';    updatePortfolioUI(profiles[currentRole]);  }  function viewPortfolioPdf() {    const url = profiles[currentRole].portfolioFile;    if (url) window.open(url, '_blank');  }  function togglePortfolioView() {    const url = profiles[currentRole].portfolioFile;    if (url) {      window.open(url, '_blank');    } else {      document.getElementById('portfolioPdfInput').click();    }  }  /* ══════════════════════════════════════════     DATABASE SYNC (Backend Only)     User profiles automatically sync to JSON     ══════════════════════════════════════════ */  async function syncProfileToDatabase() {    const profileData = { ...profiles[currentRole] };    try {      const res = await fetch(`${API_BASE}/profiles/save`, {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({          username: currentUser,          role: currentRole,          profile: profileData        })      });      const data = await res.json();      if (!res.ok || !data.success) {        console.error('Failed to sync profile:', data.message);        return false;      }      console.log('✅ Profile synced for role:', currentRole);      if (data.user) account = data.user;      return true;    } catch (err) {      console.error('Error syncing profile:', err);      return false;    }  }    /* Production API call to backend:       fetch('/api/profiles/save', {         method: 'POST',         headers: { 'Content-Type': 'application/json' },         body: JSON.stringify(profileData)       }).then(res => res.json())         .then(data => {           if (data.success) console.log('Profile synced to database');         });    */  /* ══════════════════════════════════════════     JOBS MANAGEMENT     ══════════════════════════════════════════ */  let activeJobFilter = 'all';  // Load jobs based on current role (CLIENT or FREELANCER)  async function loadJobsForRole() {    try {      const isClient = currentRole === 'client';      const endpoint = isClient        ? `${API_BASE}/jobs/my/${encodeURIComponent(currentUser)}`        : `${API_BASE}/jobs/browse/${encodeURIComponent(currentUser)}`;      const res = await fetch(endpoint);      const data = await res.json();      if (!data.success) throw new Error(data.message || 'Failed to load jobs.');      jobs = data.jobs || [];      // Cache full browse results for client-side filtering      if (!isClient) allBrowseJobs = [...jobs];      // Also load categories      const catRes = await fetch(`${API_BASE}/categories`);      const catData = await catRes.json();      if (catData.success) categories = catData.categories || [];      console.log(`✅ Loaded ${jobs.length} jobs for ${currentRole} (${currentUser}):`, jobs);      renderJobs(activeJobFilter);    } catch (err) {      console.error('Error loading jobs:', err);      jobs = [];      renderJobs(activeJobFilter);    }  }  function getCategoryName(catId) {    const cat = categories.find(c => c.id === catId);    return cat ? `${cat.icon} ${cat.name}` : catId;  }  function populateCategoryDropdown() {    const select = document.getElementById('jobCategory');    if (!select || !categories.length) return;    select.innerHTML = '<option value="">Select Category</option>';    categories.forEach(cat => {      const option = document.createElement('option');      option.value = cat.id;      option.textContent = `${cat.icon} ${cat.name}`;      select.appendChild(option);    });    select.addEventListener('change', updateSubcategoryDropdown);  }  function updateSubcategoryDropdown() {    const catId = document.getElementById('jobCategory')?.value;    const subcatSelect = document.getElementById('jobSubcategory');    if (!subcatSelect) return;    subcatSelect.innerHTML = '<option value="">Select Subcategory</option>';    if (!catId) {      subcatSelect.style.display = 'none';      return;    }    const cat = categories.find(c => c.id === catId);    if (cat && cat.subcategories) {      cat.subcategories.forEach(sub => {        const opt = document.createElement('option');        opt.value = sub;        opt.textContent = sub;        subcatSelect.appendChild(opt);      });      subcatSelect.style.display = '';    } else {      subcatSelect.style.display = 'none';    }  }  function filterJobs(filter, btn) {    activeJobFilter = filter;    document.querySelectorAll('.services-tab').forEach(t => t.classList.remove('active'));    btn.classList.add('active');    renderJobs(filter);  }  function renderJobs(filter) {    const grid = document.getElementById('jobsGrid');    if (!grid) {      console.warn('jobsGrid element not found');      return;    }    let filtered = [...(jobs || [])];    const isClient = currentRole === 'client';    console.log(`🎨 renderJobs called: filter="${filter}", total jobs=${filtered.length}, role=${currentRole}, user=${currentUser}`);    // Filter by status    if (filter && filter !== 'all') {      filtered = filtered.filter(job => job.status === filter);    }    console.log(`✂️ After status filtering (${filter}): ${filtered.length} jobs match`);    if (filtered.length === 0) {      grid.innerHTML = `        <div class="services-empty" style="grid-column:1/-1;text-align:center;padding:60px 20px;">          <div class="services-empty__icon">${isClient ? '📋' : '🔍'}</div>          <div class="services-empty__text" style="color:#7a8fa0;">            ${isClient ? 'You haven\'t posted any jobs yet.' : 'No available jobs to browse right now.'}          </div>        </div>`;      return;    }    grid.innerHTML = '';    filtered.forEach(job => {      const card = document.createElement('div');      card.className = 'service-card';      card.style.cursor = 'pointer';      // Status badge color      let badgeClass = 'dash-badge--blue';      let statusLabel = job.status;      if (job.status === 'open') { badgeClass = 'dash-badge--green'; statusLabel = 'Open'; }      else if (job.status === 'in-progress') { badgeClass = 'dash-badge--yellow'; statusLabel = 'In Progress'; }      else if (job.status === 'completed') { badgeClass = 'dash-badge--blue'; statusLabel = 'Completed'; }      else if (job.status === 'cancelled') { badgeClass = 'dash-badge--red'; statusLabel = 'Cancelled'; }      const budgetText = (job.budgetMin || job.budgetMax)        ? `₱${job.budgetMin || 0} – ₱${job.budgetMax || 0}` : 'Budget not set';      const categoryLabel = getCategoryName(job.category);      const subcatLabel = job.subcategory ? ` › ${job.subcategory}` : '';      const postedDate = job.postedAt ? new Date(job.postedAt).toLocaleDateString() : '';      const acceptedLabel = job.acceptedBy ? `<div style="font-size:0.75rem;color:#16a34a;margin-top:4px;">Accepted by: <strong>${job.acceptedBy}</strong></div>` : '';      // Action buttons      let actionButtons = '';      if (isClient) {        // Client sees Edit & Delete on their own jobs        actionButtons = `          <div style="display:flex;gap:8px;margin-top:10px;">            <button onclick="event.stopPropagation();openEditJob('${job.id}')" style="flex:1;padding:6px 12px;font-size:0.75rem;font-weight:600;font-family:Poppins,sans-serif;border:1.5px solid #1b3d7a;background:#fff;color:#1b3d7a;border-radius:8px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.background='#1b3d7a';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#1b3d7a'">✏️ Edit</button>            <button onclick="event.stopPropagation();deleteJob('${job.id}')" style="flex:1;padding:6px 12px;font-size:0.75rem;font-weight:600;font-family:Poppins,sans-serif;border:1.5px solid #ef4444;background:#fff;color:#ef4444;border-radius:8px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.background='#ef4444';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#ef4444'">🗑 Delete</button>          </div>`;      } else {        // Freelancer sees Accept button (only on open jobs not yet accepted)        if (job.status === 'open' && !job.acceptedBy) {          actionButtons = `            <div style="margin-top:10px;">              <button onclick="event.stopPropagation();acceptJobFromCard('${job.id}')" style="width:100%;padding:8px 12px;font-size:0.78rem;font-weight:700;font-family:Poppins,sans-serif;border:none;background:var(--cyan);color:#fff;border-radius:8px;cursor:pointer;box-shadow:0 2px 10px rgba(0,200,240,0.3);transition:all 0.2s;" onmouseover="this.style.background='#00afd4'" onmouseout="this.style.background='var(--cyan)'">✋ Accept Job</button>            </div>`;        } else if (job.status === 'in-progress' && job.acceptedBy === currentUser) {          actionButtons = `            <div style="margin-top:10px;">              <button onclick="event.stopPropagation();completeJobFromCard('${job.id}')" style="width:100%;padding:8px 12px;font-size:0.78rem;font-weight:700;font-family:Poppins,sans-serif;border:none;background:linear-gradient(135deg,#16a34a,#22c55e);color:#fff;border-radius:8px;cursor:pointer;box-shadow:0 2px 10px rgba(22,163,74,0.3);transition:all 0.2s;" onmouseover="this.style.opacity='0.88'" onmouseout="this.style.opacity='1'">✅ Mark as Complete</button>            </div>`;        }      }      card.innerHTML = `        <div class="service-card__img" style="display:flex;align-items:center;justify-content:center;font-size:2rem;background:linear-gradient(135deg,#e8f4fd,#d0eafb);">          ${categoryLabel.split(' ')[0] || '💼'}        </div>        <div class="service-card__body">          <div class="service-card__title" style="font-size:0.9rem;font-weight:700;color:#1a2a3a;margin-bottom:4px;">${job.title}</div>          <div style="font-size:0.75rem;color:#7a8fa0;margin-bottom:6px;">${categoryLabel}${subcatLabel}</div>          <div style="font-size:0.78rem;color:#5a6a7a;margin-bottom:6px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${job.description}</div>          <div class="service-card__price" style="font-size:0.85rem;font-weight:700;margin-bottom:4px;">${budgetText}</div>          ${job.duration ? `<div style="font-size:0.72rem;color:#8a9baa;">⏱ ${job.duration}</div>` : ''}          <div style="display:flex;align-items:center;justify-content:space-between;margin-top:8px;">            <span class="dash-badge ${badgeClass}" style="font-size:0.7rem;">${statusLabel}</span>            <span style="font-size:0.7rem;color:#aab5c0;">${postedDate}</span>          </div>          ${isClient ? '' : `<div style="font-size:0.72rem;color:#8a9baa;margin-top:4px;">Posted by: <strong>${job.postedBy}</strong></div>`}          ${acceptedLabel}          ${actionButtons}        </div>`;      grid.appendChild(card);    });  }  function openPostJob() {    // Reset form fields    resetJobForm();    document.getElementById('postJobOverlay').classList.add('active');  }  function closePostJob() {    document.getElementById('postJobOverlay').classList.remove('active');    resetJobForm();  }  let allBrowseJobs = []; // unfiltered browse results cache  function openBrowseJobs() {    if (!isBrowsingAvailableJobs) {      isBrowsingAvailableJobs = true;      activeJobFilter = 'all';      populateBrowseCategoryFilter();    }    loadJobsForRole();  }  function populateBrowseCategoryFilter() {    const sel = document.getElementById('browseCategoryFilter');    if (!sel) return;    sel.innerHTML = '<option value="">All Categories</option>';    categories.forEach(cat => {      const opt = document.createElement('option');      opt.value = cat.id;      opt.textContent = `${cat.icon} ${cat.name}`;      sel.appendChild(opt);    });  }  function updateBrowseSubcategoryFilter() {    const catId = document.getElementById('browseCategoryFilter')?.value;    const subcatSel = document.getElementById('browseSubcategoryFilter');    if (!subcatSel) return;    subcatSel.innerHTML = '<option value="">All Subcategories</option>';    if (!catId) {      subcatSel.style.display = 'none';      return;    }    const cat = categories.find(c => c.id === catId);    if (cat && cat.subcategories) {      cat.subcategories.forEach(sub => {        const opt = document.createElement('option');        opt.value = sub;        opt.textContent = sub;        subcatSel.appendChild(opt);      });      subcatSel.style.display = '';    } else {      subcatSel.style.display = 'none';    }  }  function applyBrowseFilters() {    // Update subcategory dropdown when category changes    updateBrowseSubcategoryFilter();    const keyword = (document.getElementById('browseKeyword')?.value || '').trim().toLowerCase();    const catFilter = document.getElementById('browseCategoryFilter')?.value || '';    const subcatFilter = document.getElementById('browseSubcategoryFilter')?.value || '';    let filtered = [...allBrowseJobs];    if (catFilter) {      filtered = filtered.filter(j => j.category === catFilter);    }    if (subcatFilter) {      filtered = filtered.filter(j => j.subcategory === subcatFilter);    }    if (keyword) {      filtered = filtered.filter(j => {        const haystack = `${j.title} ${j.description} ${getCategoryName(j.category)} ${j.subcategory || ''}`.toLowerCase();        return keyword.split(/\s+/).every(w => haystack.includes(w));      });    }    jobs = filtered;    renderJobs(activeJobFilter);  }  function clearBrowseFilters() {    const kw = document.getElementById('browseKeyword');    const cat = document.getElementById('browseCategoryFilter');    const subcat = document.getElementById('browseSubcategoryFilter');    if (kw) kw.value = '';    if (cat) cat.value = '';    if (subcat) { subcat.value = ''; subcat.style.display = 'none'; }    // Restore full list    if (isBrowsingAvailableJobs && allBrowseJobs.length) {      jobs = [...allBrowseJobs];      renderJobs(activeJobFilter);    }  }  function resetJobForm() {    const fields = ['jobTitle', 'jobDescription', 'jobCategory', 'jobSubcategory', 'jobBudgetMin', 'jobBudgetMax', 'jobDuration', 'jobAttachment'];    fields.forEach(id => {      const el = document.getElementById(id);      if (el) el.value = '';    });  }  async function submitJob() {    const title = document.getElementById('jobTitle')?.value.trim();    const description = document.getElementById('jobDescription')?.value.trim();    const category = document.getElementById('jobCategory')?.value;    const subcategory = document.getElementById('jobSubcategory')?.value || '';    const budgetMin = parseFloat(document.getElementById('jobBudgetMin')?.value) || 0;    const budgetMax = parseFloat(document.getElementById('jobBudgetMax')?.value) || 0;    const duration = document.getElementById('jobDuration')?.value.trim() || '';    const attachmentName = document.getElementById('jobAttachment')?.value.trim() || '';    if (!title || !description || !category) {      alert('Please fill in the job title, description, and category.');      return;    }    try {      const res = await fetch(`${API_BASE}/jobs`, {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({          username: currentUser,          title, description, category, subcategory,          budgetMin, budgetMax, duration, attachmentName        })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to post job.');      alert('Job posted successfully!');      closePostJob();      await loadJobsForRole();    } catch (err) {      alert('Error posting job: ' + err.message);    }  }  async function acceptJobFromCard(jobId) {    if (!confirm('Are you sure you want to accept this job?')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'PATCH',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ status: 'in-progress', acceptedBy: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to accept job.');      showToast('Job accepted!', 'success');      await loadJobsForRole();      loadNotifications_UI();      loadActivityCounters();    } catch (err) {      alert('Error: ' + err.message);    }  }  async function completeJobFromCard(jobId) {    if (!confirm('Mark this job as completed? The client will be notified.')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'PATCH',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ status: 'completed', completedBy: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to complete job.');      showToast('Job marked as completed!', 'success');      await loadJobsForRole();      loadNotifications_UI();      loadActivityCounters();    } catch (err) {      alert('Error: ' + err.message);    }  }  // Edit job (for clients only)  let editingJobId = null;  function openEditJob(jobId) {    const job = jobs.find(j => j.id === jobId);    if (!job) return;    editingJobId = jobId;    const editTitle = document.getElementById('editJobTitle');    const editDesc = document.getElementById('editJobDescription');    const editCat = document.getElementById('editJobCategory');    const editSubcat = document.getElementById('editJobSubcategory');    const editBudgetMin = document.getElementById('editJobBudgetMin');    const editBudgetMax = document.getElementById('editJobBudgetMax');    const editDuration = document.getElementById('editJobDuration');    const editStatus = document.getElementById('editJobStatus');    if (editTitle) editTitle.value = job.title || '';    if (editDesc) editDesc.value = job.description || '';    if (editCat) {      editCat.innerHTML = '<option value="">Select Category</option>';      categories.forEach(cat => {        const opt = document.createElement('option');        opt.value = cat.id;        opt.textContent = `${cat.icon} ${cat.name}`;        if (cat.id === job.category) opt.selected = true;        editCat.appendChild(opt);      });      // Populate subcategory for current category      updateEditSubcategoryDropdown(job.subcategory);      editCat.onchange = () => updateEditSubcategoryDropdown('');    }    if (editBudgetMin) editBudgetMin.value = job.budgetMin || '';    if (editBudgetMax) editBudgetMax.value = job.budgetMax || '';    if (editDuration) editDuration.value = job.duration || '';    if (editStatus) editStatus.value = job.status || 'open';    document.getElementById('editJobOverlay').classList.add('active');  }  function updateEditSubcategoryDropdown(preselect) {    const catSelect = document.getElementById('editJobCategory');    const subcatSelect = document.getElementById('editJobSubcategory');    if (!catSelect || !subcatSelect) return;    const selectedCat = categories.find(c => c.id === catSelect.value);    subcatSelect.innerHTML = '<option value="">Select Subcategory</option>';    if (selectedCat && selectedCat.subcategories) {      selectedCat.subcategories.forEach(sub => {        const opt = document.createElement('option');        opt.value = sub;        opt.textContent = sub;        if (sub === preselect) opt.selected = true;        subcatSelect.appendChild(opt);      });    }  }  function closeEditJob() {    document.getElementById('editJobOverlay').classList.remove('active');    editingJobId = null;  }  async function saveEditJob() {    if (!editingJobId) return;    const title = document.getElementById('editJobTitle')?.value.trim();    const description = document.getElementById('editJobDescription')?.value.trim();    const category = document.getElementById('editJobCategory')?.value;    const subcategory = document.getElementById('editJobSubcategory')?.value || '';    const budgetMin = parseFloat(document.getElementById('editJobBudgetMin')?.value) || 0;    const budgetMax = parseFloat(document.getElementById('editJobBudgetMax')?.value) || 0;    const duration = document.getElementById('editJobDuration')?.value.trim() || '';    const status = document.getElementById('editJobStatus')?.value || 'open';    if (!title || !description || !category) {      alert('Title, description, and category are required.');      return;    }    try {      const res = await fetch(`${API_BASE}/jobs/${editingJobId}`, {        method: 'PUT',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({          username: currentUser,          title, description, category, subcategory,          budgetMin, budgetMax, duration, status        })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to update job.');      alert('Job updated!');      closeEditJob();      await loadJobsForRole();    } catch (err) {      alert('Error: ' + err.message);    }  }  async function deleteJob(jobId) {    if (!confirm('Are you sure you want to delete this job? This cannot be undone.')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'DELETE',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ username: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to delete job.');      alert('Job deleted.');      await loadJobsForRole();    } catch (err) {      alert('Error: ' + err.message);    }  }  async function loadFreelancerRecommendations(jobCategory) {    try {      const res = await fetch(`${API_BASE}/freelancers/by-category/${encodeURIComponent(jobCategory)}`);      const data = await res.json();      if (data.success) return data.freelancers || [];      return [];    } catch (err) {      console.error('Error loading recommendations:', err);      return [];    }  }  document.getElementById('postJobOverlay').addEventListener('click', function(e) {     if(e.target===this) closePostJob();   });  document.addEventListener('keydown', e => {    if (e.key === 'Escape') {      document.getElementById('roleModalOverlay').classList.remove('active');      document.getElementById('editJobOverlay').classList.remove('active');      document.getElementById('postJobOverlay').classList.remove('active');    }  });let chatPartner = '';let partnerProfileOpen = false;let selectedStarRating = 0;// Toggle partner profile sidebarfunction togglePartnerProfile() {  const panel = document.getElementById('partnerProfilePanel');  partnerProfileOpen = !partnerProfileOpen;  if (partnerProfileOpen && chatPartner) {    panel.style.width = '280px';    panel.style.borderLeft = '1.5px solid #e8eff6';    loadPartnerProfile(chatPartner);  } else {    panel.style.width = '0';    panel.style.borderLeft = '0';    partnerProfileOpen = false;  }}// Load partner's profile dataasync function loadPartnerProfile(username) {  try {    const panel = document.getElementById('partnerProfilePanel');    panel.style.width = '280px';    panel.style.borderLeft = '1.5px solid #e8eff6';    const res = await fetch(`${API_BASE}/users/${encodeURIComponent(username)}`);    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { console.error('Bad JSON from user endpoint:', text); return; }    const initial = username.charAt(0).toUpperCase();    document.getElementById('ppAvatar').textContent = initial;    document.getElementById('ppName').textContent = username;    if (data.success && data.user) {      const user = data.user;      const roleData = user.roles?.freelancer || user.roles?.client || {};      document.getElementById('ppTitle').textContent = roleData.title || 'GigHub User';      document.getElementById('ppSkill').textContent = roleData.skill || 'Not specified';      document.getElementById('ppTools').textContent = roleData.tools || 'Not specified';      document.getElementById('ppLevel').textContent = roleData.level || 'Not specified';      document.getElementById('ppYears').textContent = roleData.years ? (roleData.years + ' years') : 'Not specified';      if (roleData.avatarSrc) {        document.getElementById('ppAvatar').innerHTML = `<img src="${roleData.avatarSrc}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`;      }    } else {      document.getElementById('ppTitle').textContent = 'GigHub User';      document.getElementById('ppSkill').textContent = 'Not specified';      document.getElementById('ppTools').textContent = 'Not specified';      document.getElementById('ppLevel').textContent = 'Not specified';      document.getElementById('ppYears').textContent = 'Not specified';    }    // Load reviews for this user    loadUserReviews(username);    // Reset star picker    selectedStarRating = 0;    updateStarPicker(0);    document.getElementById('ppReviewText').value = '';    // Check if current user already left a review    loadExistingReview(username);  } catch (err) {    console.error('Error loading partner profile:', err);  }}// Star pickerfunction updateStarPicker(n) {  document.querySelectorAll('.star-pick').forEach(star => {    const val = parseInt(star.dataset.star);    star.style.color = val <= n ? '#f5a623' : '#dce4ef';  });}// Hover effects for star pickerdocument.addEventListener('DOMContentLoaded', () => {  document.querySelectorAll('.star-pick').forEach(star => {    star.addEventListener('mouseenter', () => {      const hoverVal = parseInt(star.dataset.star);      document.querySelectorAll('.star-pick').forEach(s => {        s.style.color = parseInt(s.dataset.star) <= hoverVal ? '#f5a623' : '#dce4ef';      });    });    star.addEventListener('mouseleave', () => {      updateStarPicker(selectedStarRating);    });  });});// Submit a reviewasync function submitReview() {  if (!chatPartner) return;  if (selectedStarRating === 0) {    alert('Please select a star rating (1–5).');    return;  }  const reviewText = document.getElementById('ppReviewText').value.trim();  try {    const res = await fetch(`${API_BASE}/reviews`, {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({        from: currentUser,        to: chatPartner,        rating: selectedStarRating,        text: reviewText      })    });    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) {      console.error('Server response was not JSON:', text);      alert('Server error — check that your server is running and has the /api/reviews endpoint.');      return;    }    if (!res.ok || !data.success) throw new Error(data.message || 'Failed to submit review.');    showToast(data.message || '✅ Review submitted!', 'success');    loadUserReviews(chatPartner);    loadExistingReview(chatPartner);  } catch (err) {    alert('Error: ' + err.message);  }}// Load all reviews for a user (partner profile sidebar)async function loadUserReviews(username) {  try {    const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(username)}`);    if (!res.ok) { console.error('Reviews endpoint returned', res.status); return; }    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { return; }    const reviews = data.success ? (data.reviews || []) : [];    const avgContainer = document.getElementById('ppAvgRating');    const avgStars = document.getElementById('ppAvgStars');    const countEl = document.getElementById('ppRatingCount');    const listEl = document.getElementById('ppReviewsList');    if (!avgContainer || !listEl) return;    if (reviews.length === 0) {      avgContainer.textContent = '—';      avgStars.textContent = '☆☆☆☆☆';      countEl.textContent = '(0 reviews)';      listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.78rem;padding:16px 0;font-family:Poppins,sans-serif;">No reviews yet</div>';      return;    }    const avg = reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length;    avgContainer.textContent = avg.toFixed(1);    avgStars.textContent = renderStarString(Math.round(avg));    countEl.textContent = `(${reviews.length} review${reviews.length !== 1 ? 's' : ''})`;    listEl.innerHTML = reviews.map(r => {      const initial = r.from.charAt(0).toUpperCase();      const date = new Date(r.timestamp).toLocaleDateString();      const stars = renderStarString(r.rating);      return `        <div style="display:flex;gap:12px;padding:12px 0;border-bottom:1px solid #f0f4f8;">          <div style="width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.75rem;flex-shrink:0;">${initial}</div>          <div style="flex:1;">            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:3px;">              <div style="font-size:0.78rem;font-weight:700;color:#1a2a3a;">${r.from}</div>              <div style="font-size:0.62rem;color:#aab5c0;">${date}</div>            </div>            <div style="font-size:0.82rem;color:#f5a623;letter-spacing:1px;">${stars}</div>          </div>        </div>`;    }).join('');  } catch (err) {    console.error('Error loading reviews:', err);  }}// Check if current user already reviewed this personasync function loadExistingReview(username) {  try {    const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(username)}`);    if (!res.ok) return;    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { return; }    const reviews = data.success ? (data.reviews || []) : [];    const existing = reviews.find(r => r.from === currentUser);    if (existing) {      selectedStarRating = existing.rating;      updateStarPicker(existing.rating);      document.getElementById('ppReviewText').value = existing.text || '';    }  } catch (err) {    console.error('Error checking existing review:', err);  }}// Load profile reviews (for own profile page)async function loadProfileReviews() {  try {    const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(currentUser)}`);    if (!res.ok) return;    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { return; }    const reviews = data.success ? (data.reviews || []) : [];    const badge = document.getElementById('profileAvgBadge');    const listEl = document.getElementById('profileReviewsList');    if (!badge || !listEl) return;    if (reviews.length === 0) {      badge.textContent = '⭐ —';      listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:20px 0;font-family:Poppins,sans-serif;">No reviews yet. Reviews from other users will appear here.</div>';      return;    }    const avg = reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length;    badge.textContent = `⭐ ${avg.toFixed(1)}`;    listEl.innerHTML = reviews.map(r => {      const initial = r.from.charAt(0).toUpperCase();      const stars = renderStarString(r.rating);      const date = new Date(r.timestamp).toLocaleDateString();      return `        <div style="display:flex;gap:12px;padding:12px 0;border-bottom:1px solid #f0f4f8;">          <div style="width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.75rem;flex-shrink:0;">${initial}</div>          <div style="flex:1;">            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:3px;">              <div style="font-size:0.78rem;font-weight:700;color:#1a2a3a;">${r.from}</div>              <div style="font-size:0.62rem;color:#aab5c0;">${date}</div>            </div>            <div style="font-size:0.82rem;color:#f5a623;letter-spacing:1px;margin-bottom:4px;">${stars}</div>            ${r.text ? `<div style="font-size:0.75rem;color:#4a5a6a;line-height:1.5;font-family:Poppins,sans-serif;">${r.text}</div>` : ''}          </div>        </div>`;    }).join('');  } catch (err) {    console.error('Error loading profile reviews:', err);  }}// Load real activity counters from the serverasync function loadActivityCounters() {  try {    const res = await fetch(`${API_BASE}/jobs/activity/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) return;    document.getElementById('actPosted').textContent = data.posted ?? 0;    document.getElementById('actCompleted').textContent = data.completed ?? 0;    document.getElementById('actOngoing').textContent = data.ongoing ?? 0;    // Update label based on role    const label = document.getElementById('actPostedLabel');    if (label) label.textContent = currentRole === 'client' ? 'Posted' : 'Accepted';  } catch (err) {    console.error('Error loading activity counters:', err);  }}// Render star stringfunction renderStarString(n) {  return '★'.repeat(Math.max(0, Math.min(5, n))) + '☆'.repeat(Math.max(0, 5 - Math.min(5, n)));}// Load inboxfunction loadInbox() {  fetch(`${API_BASE}/usernames?exclude=${encodeURIComponent(currentUser)}`)    .then(res => res.json())    .then(data => {      const inboxList = document.getElementById('inboxList');      const usernames = (data.usernames || []).filter(username => username !== currentUser);      if (!usernames.length) {        inboxList.innerHTML = '<div style="color:#aab5c0;font-size:0.85rem;padding:20px 8px;text-align:center;font-family:Poppins,sans-serif;">No users found.</div>';        return;      }      inboxList.innerHTML = usernames.map(u => {        const initial = u.charAt(0).toUpperCase();        return `<button onclick="openChat('${u}')" id="inbox-${u}" style="          display:flex;align-items:center;gap:12px;          text-align:left;padding:10px 14px;          border-radius:10px;border:none;          background:transparent;cursor:pointer;          width:100%;font-family:Poppins,sans-serif;          transition:background 0.15s;        " onmouseover="this.style.background='#e8eff6'" onmouseout="if(!this.classList.contains('inbox-active'))this.style.background='transparent'">          <div style="width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.65rem;flex-shrink:0;">${initial}</div>          <div style="overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:0.85rem;font-weight:600;color:#1a2a3a;">${u}</div>        </button>`;      }).join('');    });}// Open chat with a userfunction openChat(username) {  document.querySelectorAll('#inboxList button').forEach(btn => {    btn.classList.remove('inbox-active');    btn.style.background = 'transparent';  });  const activeBtn = document.getElementById('inbox-' + username);  if (activeBtn) {    activeBtn.classList.add('inbox-active');    activeBtn.style.background = '#e0eaf5';  }  chatPartner = username;  const initial = username.charAt(0).toUpperCase();  document.getElementById('chatPartnerName').textContent = username;  document.getElementById('chatPartnerStatus').textContent = 'Online';  document.getElementById('chatAvatarHeader').textContent = initial;  // Show the View Profile button  document.getElementById('viewPartnerProfileBtn').style.display = 'inline-flex';  // Close profile panel when switching users  if (partnerProfileOpen) {    const panel = document.getElementById('partnerProfilePanel');    panel.style.width = '0';    panel.style.borderLeft = '0';    partnerProfileOpen = false;  }  loadMessages();}// Load messagesfunction loadMessages() {  if (!chatPartner) return;  fetch(`${API_BASE}/messages?user1=${encodeURIComponent(currentUser)}`)    .then(res => res.json())    .then(data => {      const msgs = data.messages || [];      const box = document.getElementById('chatBox');      if (!msgs.length) {        box.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.85rem;margin:auto;font-family:Poppins,sans-serif;">No messages yet. Say hello! 👋</div>';        return;      }      box.innerHTML = msgs.map(m => {        const isMe = m.from === currentUser;        const time = new Date(m.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });        return `<div style="display:flex;justify-content:${isMe ? 'flex-end' : 'flex-start'};align-items:flex-end;gap:6px;">          ${!isMe ? `<div style="width:28px;height:28px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-size:0.65rem;font-weight:700;flex-shrink:0;">${m.from.charAt(0).toUpperCase()}</div>` : ''}          <div style="max-width:65%;display:flex;flex-direction:column;align-items:${isMe ? 'flex-end' : 'flex-start'};">            <div style="              padding:10px 16px;              border-radius:${isMe ? '18px 18px 4px 18px' : '18px 18px 18px 4px'};              background:${isMe ? 'linear-gradient(135deg,#1b3d7a,#2563eb)' : '#fff'};              color:${isMe ? '#fff' : '#1a2a3a'};              font-size:0.88rem;              font-family:Poppins,sans-serif;              box-shadow:0 1px 4px rgba(0,0,0,0.06);              word-break:break-word;            ">${m.text}</div>            <div style="font-size:0.65rem;color:#aab5c0;margin-top:3px;padding:0 4px;font-family:Poppins,sans-serif;">${time}</div>          </div>        </div>`;      }).join('');      box.scrollTop = box.scrollHeight;    });}// Send messagefunction sendMessage(e) {  e.preventDefault();  const input = document.getElementById('chatInput');  const text = input.value.trim();  if (!text || !chatPartner) return false;  if (chatPartner === currentUser) {    alert('You cannot message yourself.');    return false;  }  fetch(`${API_BASE}/messages`, {    method: 'POST',    headers: { 'Content-Type': 'application/json' },    body: JSON.stringify({ from: currentUser, to: chatPartner, text })  }).then(async res => {    const data = await res.json();    if (!res.ok || !data.success) {      throw new Error(data.message || 'Could not send message.');    }    input.value = '';    loadMessages();  }).catch(err => alert(err.message));  return false;}// Load inbox on page loadwindow.addEventListener('load', () => {  loadInbox();  loadNotifications_UI();  // Poll for new notifications every 30 seconds  setInterval(loadNotifications_UI, 30000);});// ══════════════════════════════════════════// NOTIFICATIONS UI// ══════════════════════════════════════════let notifDropdownOpen = false;function toggleNotifDropdown() {  const dd = document.getElementById('notifDropdown');  notifDropdownOpen = !notifDropdownOpen;  dd.style.display = notifDropdownOpen ? 'block' : 'none';  if (notifDropdownOpen) loadNotifications_UI();}// Close dropdown when clicking outsidedocument.addEventListener('click', (e) => {  const dd = document.getElementById('notifDropdown');  const btn = document.getElementById('notifBellBtn');  if (notifDropdownOpen && dd && btn && !dd.contains(e.target) && !btn.contains(e.target)) {    notifDropdownOpen = false;    dd.style.display = 'none';  }});async function loadNotifications_UI() {  if (!currentUser) return;  try {    const res = await fetch(`${API_BASE}/notifications/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) return;    const notifs = data.notifications || [];    const unread = notifs.filter(n => !n.read).length;    // Update badge    const badge = document.getElementById('notifBadge');    if (unread > 0) {      badge.textContent = unread > 9 ? '9+' : unread;      badge.style.display = '';    } else {      badge.style.display = 'none';    }    // Render list    const listEl = document.getElementById('notifList');    if (!notifs.length) {      listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:30px 16px;font-family:Poppins,sans-serif;">No notifications yet</div>';      return;    }    listEl.innerHTML = notifs.slice(0, 30).map(n => {      const time = timeAgo(n.timestamp);      const icon = n.type === 'job-accepted' ? '✋' : n.type === 'job-completed' ? '✅' : '🔔';      const unreadDot = !n.read ? '<div style="width:8px;height:8px;border-radius:50%;background:#2563eb;flex-shrink:0;"></div>' : '';      const bg = !n.read ? 'background:#f0f6ff;' : '';      return `        <div onclick="handleNotifClick('${n.id}','${n.type}','${n.jobId || ''}')" style="display:flex;align-items:flex-start;gap:10px;padding:12px 18px;cursor:pointer;transition:background 0.15s;${bg}" onmouseover="this.style.background='#f4f7fb'" onmouseout="this.style.background='${!n.read ? '#f0f6ff' : 'transparent'}'">          <div style="font-size:1.2rem;flex-shrink:0;margin-top:2px;">${icon}</div>          <div style="flex:1;min-width:0;">            <div style="font-size:0.8rem;color:#1a2a3a;line-height:1.4;word-break:break-word;">${n.message}</div>            <div style="font-size:0.68rem;color:#8a9baa;margin-top:4px;">${time}</div>          </div>          ${unreadDot}        </div>`;    }).join('');  } catch (err) {    console.error('Error loading notifications:', err);  }}async function handleNotifClick(notifId, type, jobId) {  // Mark as read  try {    await fetch(`${API_BASE}/notifications/read`, {      method: 'PATCH',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ username: currentUser, ids: [notifId] })    });  } catch(e) {}  // Navigate to the relevant page  if (type === 'job-accepted' || type === 'job-completed') {    // Go to services/jobs page    const svcBtn = document.querySelector('[data-page=services]');    if (svcBtn) setPage(svcBtn);  }  // Close dropdown and refresh  notifDropdownOpen = false;  document.getElementById('notifDropdown').style.display = 'none';  loadNotifications_UI();}async function markAllNotifRead() {  if (!currentUser) return;  try {    await fetch(`${API_BASE}/notifications/read`, {      method: 'PATCH',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ username: currentUser })    });    loadNotifications_UI();  } catch(e) {}}function timeAgo(ts) {  const diff = Date.now() - ts;  const sec = Math.floor(diff / 1000);  if (sec < 60) return 'Just now';  const min = Math.floor(sec / 60);  if (min < 60) return min + 'm ago';  const hr = Math.floor(min / 60);  if (hr < 24) return hr + 'h ago';  const d = Math.floor(hr / 24);  if (d < 7) return d + 'd ago';  return new Date(ts).toLocaleDateString();}    function saveSettings(e, options = {}) {    if (e) e.preventDefault();    const email = document.getElementById('settingsEmail').value.trim();    const password = document.getElementById('settingsPassword').value.trim();    if (!email) {      alert('Email is required.');      return false;    }    const payload = {      email,      currentRole: options.overrideRole || currentRole    };    if ((options.persistPassword ?? true) && password) {      payload.password = password;    }    fetch(`${API_BASE}/users/${encodeURIComponent(currentUser)}/settings`, {      method: 'PATCH',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify(payload)    })    .then(async res => {      const data = await res.json();      if (!res.ok || !data.success) {        throw new Error(data.message || 'Could not update settings.');      }      account = data.user;      profiles = account.roles;      currentRole = account.currentRole || currentRole;      document.getElementById('settingsPassword').value = '';      if (!options.silent) alert('Settings updated!');    })    .catch(err => {      if (!options.silent) alert(err.message);    });    return false;  }  /* Sign out function */  function logout() {    if (!confirm('Are you sure you want to sign out?')) return;    localStorage.removeItem('gighub_user');    localStorage.removeItem('gighub_role');    window.location.href = 'login.html';  }  // SERVICE DETAILS MODAL LOGIC  function openServiceDetails(serviceId) {    const svc = (typeof services !== 'undefined' ? services : []).find(s => s.id === serviceId);    if (!svc) return;    const modal = document.getElementById('serviceDetailsOverlay');    if (!modal) return;    const titleEl = document.getElementById('svcModalTitle');    const bodyEl = document.getElementById('svcModalBody');    if (titleEl) titleEl.textContent = `${svc.emoji || ''} ${svc.title}`;    if (bodyEl) bodyEl.innerHTML = `      <div><b>Category:</b> ${svc.cat}</div>      <div><b>Price:</b> ${svc.price}</div>      <div><b>Status:</b> ${svc.status.charAt(0).toUpperCase() + svc.status.slice(1)}</div>      <div><b>Posted by:</b> ${svc.createdBy.charAt(0).toUpperCase() + svc.createdBy.slice(1)}</div>      ${svc.desc ? `<div style="margin-top:10px;"><b>Description:</b><br>${svc.desc}</div>` : ''}    `;    modal.style.display = '';  }  function closeServiceDetails() {    const modal = document.getElementById('serviceDetailsOverlay');    if (modal) modal.style.display = 'none';  }  // Optional: close modal when clicking outside  const svcOverlay = document.getElementById('serviceDetailsOverlay');  if (svcOverlay) {    svcOverlay.addEventListener('click', function(e) {      if (e.target === this) closeServiceDetails();    });  }  </script></body></html>      <button class="dash-nav__item active" data-page="dashboard" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/>          <rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/>        </svg>        Dashboard      </button>      <button class="dash-nav__item" id="navServices" data-page="services" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/>        </svg>        <span id="navServicesLabel">My Services</span>      </button>      <button class="dash-nav__item" data-page="profile" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>          <circle cx="12" cy="7" r="4"/>        </svg>        Profile      </button>      <button class="dash-nav__item" data-page="messages" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>        </svg>        Messages      </button>      <button class="dash-nav__item" data-page="settings" onclick="setPage(this)">        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">          <circle cx="12" cy="12" r="3"/>          <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/>        </svg>        Settings      </button>    </nav>    <div class="dash-sidebar__footer">GigHub</div>  </aside>  <!-- ═══════════════════════════════════════════       MAIN       ═══════════════════════════════════════════ -->  <div class="dash-main">    <!-- Topbar -->    <header class="dash-topbar">      <div class="dash-topbar__user">        <div class="dash-topbar__avatar">          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#1b3d7a" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>          </svg>        </div>        <span class="dash-topbar__username">Jane Doe</span>      </div>      <div class="dash-topbar__right">        <button class="dash-change-role" onclick="openRoleModal()">Change Role</button>        <div style="position:relative;">          <button class="dash-notif-btn" id="notifBellBtn" onclick="toggleNotifDropdown()">            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">              <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>              <path d="M13.73 21a2 2 0 0 1-3.46 0"/>            </svg>            <span class="dash-notif-badge" id="notifBadge" style="display:none;"></span>          </button>          <!-- Notification Dropdown -->          <div id="notifDropdown" style="display:none;position:absolute;top:calc(100% + 8px);right:0;width:340px;max-height:420px;background:#fff;border-radius:14px;box-shadow:0 8px 40px rgba(0,0,0,0.18);z-index:9999;overflow:hidden;font-family:Poppins,sans-serif;">            <div style="display:flex;align-items:center;justify-content:space-between;padding:16px 18px 12px;border-bottom:1.5px solid #e8eff6;">              <span style="font-weight:700;font-size:0.95rem;color:#1b3d7a;">Notifications</span>              <button onclick="markAllNotifRead()" style="background:none;border:none;font-family:Poppins,sans-serif;font-size:0.72rem;font-weight:600;color:#2563eb;cursor:pointer;padding:4px 8px;border-radius:6px;transition:background 0.15s;" onmouseover="this.style.background='#e8f0fe'" onmouseout="this.style.background='none'">Mark all read</button>            </div>            <div id="notifList" style="max-height:350px;overflow-y:auto;padding:8px 0;"></div>          </div>        </div>      </div>    </header>    <!-- ── Content ───────────────────────────── -->    <div class="dash-content">      <!-- DASHBOARD PAGE -->      <div id="page-dashboard">        <!-- Welcome banner -->        <div class="dash-welcome">          <div class="dash-welcome__text">            <h2 id="welcomeTitle">Welcome, Providers!</h2>            <p id="welcomeDesc">Connect with clients who value your expertise and grow your freelancing career with confidence.</p>          </div>        </div>        <!-- Dashboard Feed -->        <div id="dashFeed" style="margin-top:20px;">          <div id="feedLoading" style="text-align:center;padding:40px 20px;color:#7a8fa0;font-family:Poppins,sans-serif;font-size:0.88rem;">            Loading your feed...          </div>          <div id="feedContent" style="display:none;"></div>        </div>      </div><!-- /#page-dashboard -->      <!-- JOBS PAGE (Client = Post Jobs, Freelancer = Browse Jobs) -->      <div id="page-services" style="display:none;">        <!-- Job toolbar (role-dependent) -->        <div class="services-toolbar">          <!-- Client: Post Job -->          <button id="postJobBtn" class="services-add-btn" style="display:none;" onclick="openPostJob()">            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="16"/><line x1="8" y1="12" x2="16" y2="12"/></svg>            Post Job          </button>          <!-- Freelancer: Browse Jobs Search Bar -->          <div id="browseJobsSearchBar" style="display:none;flex:1;min-width:200px;max-width:420px;position:relative;">            <input type="text" id="browseKeywordToolbar" placeholder="Search jobs by keyword..."               style="width:100%;padding:10px 14px 10px 36px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;outline:none;transition:border-color 0.2s;"              onfocus="this.style.borderColor='var(--cyan)'" onblur="this.style.borderColor='#d0dbe6'"              oninput="document.getElementById('browseKeyword').value=this.value;applyBrowseFilters()" />            <svg style="position:absolute;left:12px;top:50%;transform:translateY(-50%);opacity:0.4;" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>          </div>        </div>        <!-- Header bar -->        <div class="services-header-bar" id="jobsHeaderBar">My Jobs</div>        <!-- Freelancer Browse Search/Filter Bar -->        <div id="browseSearchBar" style="display:none;padding:12px 0;">          <div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center;">            <div style="flex:1;min-width:200px;position:relative;">              <input type="text" id="browseKeyword" placeholder="Search jobs by keyword..."                 style="width:100%;padding:10px 14px 10px 36px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;outline:none;transition:border-color 0.2s;"                onfocus="this.style.borderColor='var(--cyan)'" onblur="this.style.borderColor='#d0dbe6'"                oninput="applyBrowseFilters()" />              <svg style="position:absolute;left:12px;top:50%;transform:translateY(-50%);opacity:0.4;" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>            </div>            <select id="browseCategoryFilter"               style="padding:10px 14px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;background:#fff;cursor:pointer;min-width:180px;outline:none;"              onchange="applyBrowseFilters()">              <option value="">All Categories</option>            </select>            <select id="browseSubcategoryFilter"               style="padding:10px 14px;border:1.5px solid #d0dbe6;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;background:#fff;cursor:pointer;min-width:180px;outline:none;display:none;"              onchange="applyBrowseFilters()">              <option value="">All Subcategories</option>            </select>            <button onclick="clearBrowseFilters()"               style="padding:10px 16px;border:1.5px solid #ef4444;background:#fff;color:#ef4444;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.78rem;font-weight:600;cursor:pointer;transition:all 0.2s;white-space:nowrap;"              onmouseover="this.style.background='#ef4444';this.style.color='#fff'"               onmouseout="this.style.background='#fff';this.style.color='#ef4444'">✕ Clear</button>          </div>        </div>        <!-- Filter tabs -->        <div class="services-tabs" id="jobFilterTabs">          <button class="services-tab active" onclick="filterJobs('all', this)">All</button>          <button class="services-tab" onclick="filterJobs('open', this)">Open</button>          <button class="services-tab" onclick="filterJobs('in-progress', this)">In Progress</button>          <button class="services-tab" onclick="filterJobs('completed', this)">Completed</button>        </div>        <!-- Jobs grid -->        <div class="services-grid" id="jobsGrid">          <!-- Populated by JS -->        </div>      </div><!-- /#page-services -->      <!-- PROFILE PAGE — role-aware, fully editable -->      <div id="page-profile" style="display:none;">        <div class="profile-page" id="profilePage">          <!-- ── Hero card ── -->          <div class="profile-hero">            <div class="profile-hero__banner"></div>            <div class="profile-hero__body">              <div class="profile-hero__avatar-wrap">                <div class="profile-hero__avatar" id="profileAvatarDisplay">👤</div>                <div class="profile-hero__avatar-edit" onclick="document.getElementById('avatarInput').click()" title="Change photo">                  <svg viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>                </div>                <input type="file" id="avatarInput" accept="image/*" style="display:none" onchange="handleAvatarChange(event)" />              </div>              <div class="profile-hero__info">                <div>                  <div class="profile-hero__name" id="profileNameDisplay">Jane Doe</div>                  <div class="profile-hero__title" id="profileTitleDisplay">Software Engineer &amp; UI/UX Designer</div>                  <div class="profile-hero__since">Member since 2023</div>                </div>                <div style="display:flex;align-items:flex-start;gap:24px;flex-wrap:wrap;">                  <div class="profile-hero__meta" id="profileMetaDisplay">                    English and Tagalog<br>Manila, Philippines                  </div>                  <button class="profile-edit-btn" id="editProfileBtn" onclick="toggleProfileEdit()">                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>                    Edit                  </button>                </div>              </div>            </div>            <div class="profile-hero__bio" id="profileBioDisplay">              I help startups and small businesses turn ideas into clean, user-friendly interfaces. With 5+ years of experience in product design, I specialize in mobile apps, dashboards, and landing pages.            </div>          </div>          <!-- ── Main 2-col grid ── -->          <div class="profile-grid">            <!-- LEFT: Skills & Tools -->            <div class="profile-panel" id="panelSkills">              <div class="profile-panel__header">                <span class="profile-panel__title">Skills and Tools</span>              </div>              <div class="profile-panel__body">                <div class="prof-field">                  <div class="prof-field__label">Primary skill</div>                  <input class="prof-field__value" id="pfSkill" value="UI/UX Design" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Frequently Used Tools</div>                  <input class="prof-field__value" id="pfTools" value="Figma, Adobe XD, Sketch" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Skill proficiency level</div>                  <input class="prof-field__value" id="pfLevel" value="Expert" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Years of experience</div>                  <input class="prof-field__value" id="pfYears" value="6 years" readonly />                </div>              </div>            </div>            <!-- RIGHT: Reviews -->            <div class="profile-panel">              <div class="profile-panel__header">                <span class="profile-panel__title">Reviews</span>                <div class="profile-panel__badge" id="profileAvgBadge">⭐ —</div>              </div>              <div class="profile-panel__body">                <div id="profileReviewsList">                  <div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:20px 0;font-family:Poppins,sans-serif;">No reviews yet. Reviews from other users will appear here.</div>                </div>              </div>            </div>            <!-- LEFT BOTTOM: Rates + Portfolio + Activity -->            <div style="display:flex;flex-direction:column;gap:20px;">              <!-- Rates and Payments -->              <div class="profile-panel">                <div class="profile-panel__header">                  <span class="profile-panel__title">Rates and Payments</span>                </div>                <div class="profile-panel__body">                  <div class="rates-row">                    <span class="rates-label">Hourly Rate</span>                    <span style="font-size:.78rem;color:#8a9baa;">₱</span>                    <input class="rates-pill" id="pfHourly" value="3,800" readonly style="width:80px;text-align:center;" />                    <span style="font-size:.78rem;color:#8a9baa;">/hr</span>                  </div>                  <div class="rates-row">                    <span class="rates-label">Fixed Project Rate</span>                    <span style="font-size:.78rem;color:#8a9baa;">₱</span>                    <input class="rates-pill" id="pfFixedMin" value="46,000" readonly style="width:88px;text-align:center;" />                    <span class="rates-sep">–</span>                    <input class="rates-pill" id="pfFixedMax" value="464,000" readonly style="width:96px;text-align:center;" />                  </div>                  <div class="rates-row">                    <span class="rates-label">Preferred Payment Method</span>                    <input class="rates-pill" id="pfPayment" value="Bank Transfer/E-Wallet" readonly style="width:auto;flex:1;" />                  </div>                </div>              </div>              <!-- Portfolio -->              <div class="profile-panel">                <div class="profile-panel__header">                  <span class="profile-panel__title">Portfolio</span>                </div>                <div class="profile-panel__body">                  <button class="portfolio-btn" onclick="togglePortfolioView()">                    <span>View Full Portfolio</span>                    <div class="portfolio-btn__icon">                      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">                        <polyline points="15 3 21 3 21 9"/><polyline points="9 21 3 21 3 15"/>                        <line x1="21" y1="3" x2="14" y2="10"/><line x1="3" y1="21" x2="10" y2="14"/>                      </svg>                    </div>                  </button>                  <!-- No portfolio yet -->                  <div id="portfolioEmpty" style="display:block;">                    <div class="portfolio-upload" onclick="document.getElementById('portfolioPdfInput').click()">                      <div class="portfolio-upload__icon">📄</div>                      <div class="portfolio-upload__text">                        No portfolio uploaded yet.<br>                        <span class="portfolio-upload__link">Click to upload a PDF</span> of your professional portfolio.                      </div>                    </div>                    <input type="file" id="portfolioPdfInput" accept="application/pdf" style="display:none" onchange="handlePortfolioUpload(event)" />                  </div>                  <!-- Portfolio uploaded -->                  <div id="portfolioFile" style="display:none;">                    <div class="portfolio-file-display">                      <div class="portfolio-file-icon">📄</div>                      <div class="portfolio-file-name" id="portfolioFileName">portfolio.pdf</div>                      <button class="portfolio-file-remove" onclick="removePortfolio()">✕ Remove</button>                    </div>                    <div style="display:flex;gap:10px;margin-top:10px;">                      <button onclick="viewPortfolioPdf()" style="flex:1;background:var(--cyan);color:#fff;border:none;border-radius:999px;padding:8px 12px;font-family:Poppins,sans-serif;font-size:.75rem;font-weight:700;cursor:pointer;">View PDF</button>                      <button onclick="document.getElementById('portfolioPdfInput').click()" style="flex:1;background:#f0f4f8;color:#4a5a6a;border:none;border-radius:999px;padding:8px 12px;font-family:Poppins,sans-serif;font-size:.75rem;font-weight:700;cursor:pointer;">Replace</button>                    </div>                  </div>                </div>              </div>              <!-- Activity -->              <div class="profile-panel">                <div class="profile-panel__header">                  <span class="profile-panel__title">Activity</span>                </div>                <div class="profile-panel__body">                  <div class="activity-boxes">                    <div class="activity-box"><div class="activity-box__num" id="actPosted">0</div><div class="activity-box__lbl" id="actPostedLabel">Posted</div></div>                    <div class="activity-box"><div class="activity-box__num" id="actCompleted">0</div><div class="activity-box__lbl">Completed</div></div>                    <div class="activity-box"><div class="activity-box__num" id="actOngoing">0</div><div class="activity-box__lbl">In Progress</div></div>                  </div>                </div>              </div>            </div><!-- /left bottom stack -->            <!-- RIGHT BOTTOM: Education & Credentials -->            <div class="profile-panel">              <div class="profile-panel__header">                <span class="profile-panel__title">Education and Credentials</span>              </div>              <div class="profile-panel__body">                <div class="prof-field">                  <div class="prof-field__label">Degree / Field of Study</div>                  <input class="prof-field__value" id="pfDegree" value="B.S. in Computer Science" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Institution</div>                  <input class="prof-field__value" id="pfInstitution" value="University of the Philippines" readonly />                </div>                <div class="prof-field">                  <div class="prof-field__label">Year of Graduation</div>                  <input class="prof-field__value" id="pfGradYear" value="2019" readonly />                </div>                <div class="prof-field" style="margin-top:18px;">                  <div class="prof-field__label">Certifications</div>                  <input class="prof-field__value" id="pfCerts" value="Google UX Design Certificate" readonly />                </div>              </div>            </div>          </div><!-- /.profile-grid -->        </div><!-- /.profile-page -->      </div><!-- /#page-profile -->            <!-- MESSAGES PAGE (chat demo) -->      <div id="page-messages" style="display:none;">        <div style="display:flex;gap:0;height:calc(100vh - 120px);max-height:calc(100vh - 120px);background:#fff;border-radius:16px;box-shadow:0 2px 16px rgba(0,0,0,0.07);overflow:hidden;">                    <!-- Inbox Sidebar -->          <div style="width:260px;min-width:220px;border-right:1.5px solid #e8eff6;display:flex;flex-direction:column;background:#f8fafc;">            <div style="padding:20px 18px 14px;font-weight:700;font-size:1.05rem;color:#1b3d7a;border-bottom:1.5px solid #e8eff6;font-family:Poppins,sans-serif;">              💬 Inbox            </div>            <div id="inboxList" style="flex:1;overflow-y:auto;padding:10px 10px;display:flex;flex-direction:column;gap:4px;"></div>          </div>          <!-- Chat Area -->          <div style="flex:1;display:flex;flex-direction:column;min-width:0;">                        <!-- Chat Header -->            <div style="padding:16px 22px;border-bottom:1.5px solid #e8eff6;display:flex;align-items:center;gap:12px;background:#fff;">              <div id="chatAvatarHeader" style="width:36px;height:36px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.85rem;font-family:Poppins,sans-serif;flex-shrink:0;">                ?              </div>              <div style="flex:1;">                <div id="chatPartnerName" style="font-weight:700;font-size:0.95rem;color:#1a2a3a;font-family:Poppins,sans-serif;"></div>                <div id="chatPartnerStatus" style="font-size:0.72rem;color:#7a8fa0;font-family:Poppins,sans-serif;">Select a conversation</div>              </div>              <!-- View Profile Button -->              <button id="viewPartnerProfileBtn" onclick="togglePartnerProfile()" style="display:none;align-items:center;gap:6px;border:1.5px solid #1b3d7a;background:#fff;color:#1b3d7a;font-family:Poppins,sans-serif;font-weight:600;font-size:0.72rem;border-radius:999px;padding:6px 14px;cursor:pointer;transition:all 0.2s;white-space:nowrap;" onmouseover="this.style.background='#1b3d7a';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#1b3d7a'">                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>                View Profile              </button>            </div>            <!-- Messages Container -->            <div id="chatBox" style="flex:1;overflow-y:auto;padding:20px 22px;display:flex;flex-direction:column;gap:10px;background:#f4f7fb;">              <div style="text-align:center;color:#aab5c0;font-size:0.85rem;margin:auto;font-family:Poppins,sans-serif;">                👈 Select a user from the inbox to start chatting              </div>            </div>            <!-- Message Input -->            <form id="chatForm" onsubmit="return sendMessage(event)" style="padding:14px 18px;border-top:1.5px solid #e8eff6;display:flex;gap:10px;align-items:center;background:#fff;">              <input id="chatInput" type="text" placeholder="Type a message…" autocomplete="off"                 style="flex:1;padding:11px 16px;border-radius:999px;border:1.5px solid #dce4ef;font-size:0.88rem;font-family:Poppins,sans-serif;outline:none;background:#f8fafc;transition:border-color 0.2s;"                onfocus="this.style.borderColor='#1b3d7a'" onblur="this.style.borderColor='#dce4ef'" />              <button type="submit" style="background:linear-gradient(135deg,#1b3d7a,#2563eb);color:#fff;border:none;border-radius:50%;width:42px;height:42px;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0;box-shadow:0 2px 10px rgba(27,61,122,0.25);transition:transform 0.15s;">                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">                  <line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/>                </svg>              </button>            </form>          </div>          <!-- Partner Profile Sidebar (hidden by default) -->          <div id="partnerProfilePanel" style="width:0;overflow:hidden;border-left:0;background:#f8fafc;display:flex;flex-direction:column;transition:width 0.3s ease, border-left 0.3s ease, padding 0.3s ease;">            <div id="partnerProfileContent" style="width:280px;padding:0;display:flex;flex-direction:column;gap:0;overflow-y:auto;">                            <!-- Profile Header -->              <div style="padding:22px 20px 16px;border-bottom:1.5px solid #e8eff6;text-align:center;">                <div id="ppAvatar" style="width:64px;height:64px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:1.4rem;font-family:Poppins,sans-serif;margin:0 auto 10px;"></div>                <div id="ppName" style="font-weight:700;font-size:1rem;color:#1a2a3a;font-family:Poppins,sans-serif;"></div>                <div id="ppTitle" style="font-size:0.75rem;color:#7a8fa0;font-family:Poppins,sans-serif;margin-top:2px;"></div>                <button onclick="togglePartnerProfile()" style="margin-top:10px;background:none;border:1.5px solid #dce4ef;border-radius:999px;padding:5px 14px;font-family:Poppins,sans-serif;font-size:0.7rem;font-weight:600;color:#7a8fa0;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.borderColor='#ef4444';this.style.color='#ef4444'" onmouseout="this.style.borderColor='#dce4ef';this.style.color='#7a8fa0'">✕ Close</button>              </div>              <!-- Skills Info -->              <div style="padding:18px 20px;border-bottom:1.5px solid #e8eff6;">                <div style="font-weight:700;font-size:0.8rem;color:#1b3d7a;font-family:Poppins,sans-serif;margin-bottom:14px;text-transform:uppercase;letter-spacing:0.06em;">Skills & Expertise</div>                                <div style="margin-bottom:12px;">                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Primary Skill</div>                  <div id="ppSkill" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>                <div style="margin-bottom:12px;">                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Frequently Used Tools</div>                  <div id="ppTools" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>                <div style="margin-bottom:12px;">                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Skill Proficiency Level</div>                  <div id="ppLevel" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>                <div>                  <div style="font-size:0.68rem;font-weight:600;color:#8a9baa;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">Years of Experience</div>                  <div id="ppYears" style="font-size:0.82rem;font-weight:600;color:#1a2a3a;font-family:Poppins,sans-serif;background:#f0f4fb;border-radius:8px;padding:8px 12px;">—</div>                </div>              </div>              <!-- Rating Section -->              <div style="padding:18px 20px;border-bottom:1.5px solid #e8eff6;">                <div style="font-weight:700;font-size:0.8rem;color:#1b3d7a;font-family:Poppins,sans-serif;margin-bottom:6px;text-transform:uppercase;letter-spacing:0.06em;">Average Rating</div>                <div style="display:flex;align-items:center;gap:8px;margin-bottom:14px;">                  <div id="ppAvgRating" style="font-size:1.6rem;font-weight:800;color:#f5a623;font-family:Poppins,sans-serif;">—</div>                  <div id="ppAvgStars" style="font-size:1.1rem;color:#f5a623;letter-spacing:2px;">☆☆☆☆☆</div>                  <div id="ppRatingCount" style="font-size:0.7rem;color:#8a9baa;font-family:Poppins,sans-serif;">(0 reviews)</div>                </div>                <!-- Your Rating -->                <div style="background:#f7fbff;border-radius:12px;padding:14px 16px;">                  <div style="font-size:0.72rem;font-weight:600;color:#5a6a7a;margin-bottom:8px;font-family:Poppins,sans-serif;">Your Rating</div>                  <div id="ppStarPicker" style="display:flex;gap:4px;margin-bottom:10px;">                    <span class="star-pick" data-star="1" onclick="pickStar(1)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="2" onclick="pickStar(2)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="3" onclick="pickStar(3)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="4" onclick="pickStar(4)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                    <span class="star-pick" data-star="5" onclick="pickStar(5)" style="font-size:1.6rem;cursor:pointer;color:#dce4ef;transition:color 0.15s;">★</span>                  </div>                  <textarea id="ppReviewText" placeholder="Write a short review (optional)…" style="width:100%;border:1.5px solid #dce4ef;border-radius:8px;padding:8px 12px;font-family:Poppins,sans-serif;font-size:0.78rem;outline:none;resize:vertical;min-height:60px;transition:border-color 0.2s;" onfocus="this.style.borderColor='#1b3d7a'" onblur="this.style.borderColor='#dce4ef'"></textarea>                  <button onclick="submitReview()" style="margin-top:8px;width:100%;padding:9px;background:linear-gradient(135deg,#1b3d7a,#2563eb);color:#fff;border:none;border-radius:999px;font-family:Poppins,sans-serif;font-weight:700;font-size:0.75rem;letter-spacing:0.06em;text-transform:uppercase;cursor:pointer;box-shadow:0 2px 10px rgba(27,61,122,0.25);transition:opacity 0.2s;" onmouseover="this.style.opacity='0.88'" onmouseout="this.style.opacity='1'">Submit Review</button>                </div>              </div>              <!-- Reviews List -->              <div style="padding:18px 20px;">                <div style="font-weight:700;font-size:0.8rem;color:#1b3d7a;font-family:Poppins,sans-serif;margin-bottom:12px;text-transform:uppercase;letter-spacing:0.06em;">Reviews</div>                <div id="ppReviewsList" style="display:flex;flex-direction:column;gap:10px;">                  <div style="text-align:center;color:#aab5c0;font-size:0.78rem;padding:16px 0;font-family:Poppins,sans-serif;">No reviews yet</div>                </div>              </div>            </div>          </div>        </div>      </div>      <!-- SETTINGS PAGE (update email/password) -->      <div id="page-settings" style="display:none;">        <div class="dash-panel" style="max-width:600px;">          <div class="dash-panel__header"><span class="dash-panel__title">Settings</span></div>          <form id="settingsForm" onsubmit="return saveSettings(event)" style="display:flex;flex-direction:column;gap:18px;margin-top:18px;">            <label>              <span style="font-size:.95rem;color:#1a2a3a;">Email Address</span>              <input id="settingsEmail" type="email" required style="width:100%;padding:10px 14px;border-radius:8px;border:1.5px solid #e0e8f0;font-size:.95rem;" />            </label>            <label>              <span style="font-size:.95rem;color:#1a2a3a;">New Password</span>              <input id="settingsPassword" type="password" minlength="6" placeholder="Leave blank to keep current" style="width:100%;padding:10px 14px;border-radius:8px;border:1.5px solid #e0e8f0;font-size:.95rem;" />            </label>            <button type="submit" style="background:#1b3d7a;color:#fff;border:none;border-radius:8px;padding:10px 0;font-weight:700;cursor:pointer;">Save Changes</button>          </form>          <div style="margin-top:30px;padding-top:20px;border-top:1px solid #e0e8f0;">            <button onclick="logout()" style="width:100%;background:#f44336;color:#fff;border:none;border-radius:8px;padding:12px 0;font-weight:700;cursor:pointer;font-size:.95rem;">Sign Out</button>          </div>        </div>      </div>    </div><!-- /.dash-content -->  </div><!-- /.dash-main --></div><!-- /.dash-shell --><!-- ═══════════════════════════════════════════     ROLE SWITCH MODAL     ═══════════════════════════════════════════ --><div class="role-modal-overlay" id="roleModalOverlay">  <div class="role-modal">    <div class="role-modal__title">Switch Your Role</div>    <div class="role-modal__sub">Choose how you want to use GigHub right now.</div>    <div class="role-modal__options">      <div class="role-option" id="roleOptClient" onclick="selectRole('client')">        <div class="role-option__icon">🏢</div>        <div class="role-option__label">Client</div>        <div class="role-option__desc">Post jobs & hire talent</div>      </div>      <div class="role-option selected" id="roleOptFreelancer" onclick="selectRole('freelancer')">        <div class="role-option__icon">💼</div>        <div class="role-option__label">Freelancer</div>        <div class="role-option__desc">Offer services & earn</div>      </div>    </div>    <button class="role-modal__confirm" onclick="confirmRole()">Confirm Role</button>  </div></div><!-- EDIT JOB MODAL (Client) --><div class="role-modal-overlay" id="editJobOverlay">  <div class="role-modal" style="width:520px;max-height:90vh;overflow-y:auto;">    <div class="role-modal__title">Edit Job</div>    <div class="role-modal__sub">Update your job details</div>    <div style="display:flex;flex-direction:column;gap:14px;margin-bottom:20px;">      <input id="editJobTitle" class="auth-field__input" type="text" placeholder="Job title" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />            <textarea id="editJobDescription" placeholder="Job description" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;resize:vertical;min-height:100px;"></textarea>            <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">        <input id="editJobBudgetMin" type="number" placeholder="Min Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />        <input id="editJobBudgetMax" type="number" placeholder="Max Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />      </div>            <input id="editJobDuration" type="text" placeholder="Expected duration (e.g. 2 weeks)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />    </div>    <div style="display:flex;gap:12px;">      <button class="role-modal__confirm" onclick="saveEditJob()" style="flex:1;">Save Changes</button>      <button onclick="closeEditJob()" style="flex:1;border:2px solid #e0e8f0;background:#fff;border-radius:999px;padding:13px;font-family:Poppins,sans-serif;font-weight:700;font-size:.85rem;cursor:pointer;color:#7a8fa0;">Cancel</button>    </div>  </div></div><!-- POST JOB MODAL (Client) --><div class="role-modal-overlay" id="postJobOverlay">  <div class="role-modal" style="width:520px;max-height:90vh;overflow-y:auto;">    <div class="role-modal__title">Post a New Job</div>    <div class="role-modal__sub">Tell freelancers what you need done</div>    <div style="display:flex;flex-direction:column;gap:14px;margin-bottom:20px;">      <input id="jobTitle" class="auth-field__input" type="text" placeholder="Job title (e.g. Logo Design)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />            <select id="jobCategory" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;color:#4a5a6a;">        <option value="">Select Category</option>        <!-- Populated by JS -->      </select>            <select id="jobSubcategory" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;color:#4a5a6a;">        <option value="">Select Subcategory</option>        <!-- Populated by JS -->      </select>            <textarea id="jobDescription" placeholder="Job description - what do you need?" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;resize:vertical;min-height:100px;"></textarea>            <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">        <input id="jobBudgetMin" type="number" placeholder="Min Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />        <input id="jobBudgetMax" type="number" placeholder="Max Budget (₱)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />      </div>            <input id="jobDuration" type="text" placeholder="Expected duration (e.g. 2 weeks)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />            <input id="jobAttachment" type="text" placeholder="Attachment filename (optional)" style="border:1.5px solid #e0e8f0;border-radius:8px;padding:10px 14px;font-family:Poppins,sans-serif;font-size:.86rem;outline:none;" />    </div>    <div style="display:flex;gap:12px;">      <button class="role-modal__confirm" onclick="submitJob()" style="flex:1;">Post Job</button>      <button onclick="closePostJob()" style="flex:1;border:2px solid #e0e8f0;background:#fff;border-radius:999px;padding:13px;font-family:Poppins,sans-serif;font-weight:700;font-size:.85rem;cursor:pointer;color:#7a8fa0;">Cancel</button>    </div>  </div></div><script>  /* ══════════════════════════════════════════     STATE     ══════════════════════════════════════════ */  const API_BASE = '/api';  let currentUser = null;  let currentRole = 'freelancer';  let pendingRole = 'freelancer';  let isEditingProfile = false;  let portfolioObjectUrl = null;  let account = null;  let isBrowsingAvailableJobs = false;  let jobs = [];  let categories = [];  /* Independent profiles per user/role with credentials */  let profiles = { freelancer: {}, client: {} };  /* ══════════════════════════════════════════     PAGE NAVIGATION     ══════════════════════════════════════════ */  function setPage(btn) {    const page = btn.dataset.page;    console.log('🔖 setPage called with:', page);    document.querySelectorAll('.dash-nav__item').forEach(el => el.classList.remove('active'));    btn.classList.add('active');    // Find current visible page and target page    const allPages = document.querySelectorAll('[id^="page-"]');    let outEl = null;    allPages.forEach(el => {      if (el.style.display !== 'none' && el.id !== 'page-' + page) outEl = el;    });    const inEl = document.getElementById('page-' + page);    // Use animated transition    if (typeof GigHubAnimations !== 'undefined') {      GigHubAnimations.transitionPages(outEl, inEl, () => {        // Hide all other pages (safety)        allPages.forEach(el => { if (el !== inEl) el.style.display = 'none'; });      }).then(() => {        afterPageSwitch(page);      });    } else {      // Fallback: no animation      allPages.forEach(el => el.style.display = 'none');      inEl.style.display = '';      afterPageSwitch(page);    }  }  function afterPageSwitch(page) {    if (page === 'dashboard') {      loadDashboardFeed();      if (typeof GigHubAnimations !== 'undefined') GigHubAnimations.animateWelcome();    }    if (page === 'services') {      console.log('📂 Switching to Services tab - will load and render jobs');      isBrowsingAvailableJobs = false;      activeJobFilter = 'all';      const searchBar = document.getElementById('browseSearchBar');      if (searchBar) searchBar.style.display = 'none';      loadJobsForRole().then(() => {        console.log('✅ loadJobsForRole completed, now rendering...');        renderJobs(activeJobFilter);        updateJobButtons();        document.querySelectorAll('.services-tab').forEach(t => t.classList.remove('active'));        document.querySelectorAll('.services-tab')[0]?.classList.add('active');        if (typeof GigHubAnimations !== 'undefined') {          GigHubAnimations.staggerChildren('#jobsGrid', '.service-card');        }      }).catch(err => {        console.error('❌ Error loading jobs:', err);        renderJobs(activeJobFilter);      });    }    if (page === 'profile')  renderProfile();    if (page === 'messages') {      loadInbox();      document.getElementById('chatBox').innerHTML = '';      document.getElementById('chatPartnerName').textContent = '';      chatPartner = '';    }    if (page === 'settings') {      document.getElementById('settingsEmail').value = account.email || '';      document.getElementById('settingsPassword').value = '';    }  }  /* ══════════════════════════════════════════     PAGE INITIALIZATION     ══════════════════════════════════════════ */  window.addEventListener('load', async () => {    const sessionUser = localStorage.getItem('gighub_user');    if (!sessionUser) {      window.location.href = 'login.html';      return;    }    currentUser = sessionUser;    // Load user data from server first    try {      const res = await fetch(`${API_BASE}/users/${encodeURIComponent(currentUser)}`);      const data = await res.json();      if (data.success && data.user) {        account = data.user;        currentRole = account.currentRole || 'freelancer';        pendingRole = currentRole;        // Load both role profiles from server — preserve both        if (account.roles?.freelancer) {          profiles.freelancer = { ...profiles.freelancer, ...account.roles.freelancer };        }        if (account.roles?.client) {          profiles.client = { ...profiles.client, ...account.roles.client };        }        // Populate settings fields        const settingsEmail = document.getElementById('settingsEmail');        if (settingsEmail) settingsEmail.value = account.email || '';      }    } catch (err) {      console.error('Error loading user data:', err);    }    try {      await loadJobsForRole();    } catch (err) {      console.error('Error loading jobs:', err);    }    renderProfile();    applyTheme(currentRole);    updateJobButtons();    populateCategoryDropdown();    loadDashboardFeed();  });  /* Update job buttons based on current role */  function updateJobButtons() {    const isFreelancer = currentRole === 'freelancer';    const postBtn = document.getElementById('postJobBtn');    const browseSearchBar = document.getElementById('browseJobsSearchBar');    const filterBar = document.getElementById('browseSearchBar');        if (!postBtn || !browseSearchBar) {      console.warn('Job buttons not found in DOM');      return;    }        if (isFreelancer) {      postBtn.style.display = 'none';      browseSearchBar.style.display = 'flex';      // Auto-enter browse mode      if (!isBrowsingAvailableJobs) {        isBrowsingAvailableJobs = true;        populateBrowseCategoryFilter();      }      if (filterBar) filterBar.style.display = '';    } else {      postBtn.style.display = 'inline-flex';      browseSearchBar.style.display = 'none';      postBtn.style.borderColor = '#f08020';      postBtn.style.color = '#f08020';      if (filterBar) filterBar.style.display = 'none';      isBrowsingAvailableJobs = false;    }  }  /* ══════════════════════════════════════════     ROLE MODAL     ══════════════════════════════════════════ */  function openRoleModal() {    pendingRole = currentRole;    highlightRole(pendingRole);    document.getElementById('roleModalOverlay').classList.add('active');  }  function selectRole(r) { pendingRole = r; highlightRole(r); }  function highlightRole(r) {    document.getElementById('roleOptClient').classList.toggle('selected',     r === 'client');    document.getElementById('roleOptFreelancer').classList.toggle('selected', r === 'freelancer');  }  function confirmRole() {    // Save current profile data BEFORE switching    if (isEditingProfile) {      exitProfileEdit();    }    currentRole = pendingRole;    document.getElementById('roleModalOverlay').classList.remove('active');    applyTheme(currentRole);    updateJobButtons();    saveSettings(null, { silent: true, persistPassword: false, overrideRole: currentRole });        // Reset filter and reload jobs for new role    activeJobFilter = 'all';    loadJobsForRole().then(() => {      document.querySelectorAll('.services-tab')[0]?.classList.add('active');    }).catch(err => {      console.error('Error loading jobs after role change:', err);    });        // Reload feed for new role    loadDashboardFeed();    // Re-render profile for the NEW role (using its own saved data)    if (document.getElementById('page-profile').style.display !== 'none') renderProfile();  }  document.getElementById('roleModalOverlay').addEventListener('click', function(e) {    if (e.target === this) this.classList.remove('active');  });    document.getElementById('editJobOverlay').addEventListener('click', function(e) {    if (e.target === this) this.classList.remove('active');  });    document.getElementById('postJobOverlay').addEventListener('click', function(e) {    if (e.target === this) this.classList.remove('active');  });  /* ══════════════════════════════════════════     THEME & DASHBOARD CONTENT     ══════════════════════════════════════════ */  function applyTheme(role) {    const shell = document.getElementById('dashShell');    const isFL = role === 'freelancer';    shell.setAttribute('data-role', role);    const avatarSvg = document.querySelector('.dash-topbar__avatar svg');    if (avatarSvg) avatarSvg.setAttribute('stroke', isFL ? '#1b3d7a' : '#c06010');    document.getElementById('welcomeTitle').textContent = isFL ? 'Welcome, Providers!' : 'Welcome, Seekers!';    document.getElementById('welcomeDesc').textContent  = isFL      ? 'Connect with clients who value your expertise and grow your freelancing career with confidence.'      : 'Work with skilled professionals who can bring your ideas to life — efficiently and reliably';  }  /* ══════════════════════════════════════════     DASHBOARD FEED     ══════════════════════════════════════════ */  async function loadDashboardFeed() {    const loading = document.getElementById('feedLoading');    const content = document.getElementById('feedContent');    if (!loading || !content) return;    loading.style.display = '';    content.style.display = 'none';    try {      if (currentRole === 'freelancer') {        await renderFreelancerFeed(content);      } else {        await renderClientFeed(content);      }      loading.style.display = 'none';      content.style.display = '';    } catch (err) {      console.error('Feed load error:', err);      loading.textContent = 'Failed to load feed.';    }  }  async function renderFreelancerFeed(container) {    const res = await fetch(`${API_BASE}/feed/freelancer/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) throw new Error(data.message);    const feedJobs = data.jobs || [];    const feedCats = data.categories || [];    function catName(id) {      const c = feedCats.find(cat => cat.id === id);      return c ? `${c.icon} ${c.name}` : id;    }    if (feedJobs.length === 0) {      container.innerHTML = `        <div style="text-align:center;padding:50px 20px;">          <div style="font-size:2.5rem;margin-bottom:12px;">🔍</div>          <div style="font-size:1rem;font-weight:600;color:#1a2a3a;margin-bottom:6px;font-family:Poppins,sans-serif;">No jobs available right now</div>          <div style="font-size:0.82rem;color:#7a8fa0;font-family:Poppins,sans-serif;">Check back later for new opportunities!</div>        </div>`;      return;    }    container.innerHTML = `      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px;">        <h3 style="font-family:Poppins,sans-serif;font-size:1.05rem;font-weight:700;color:#1b3d7a;margin:0;">💼 Jobs For You</h3>        <button onclick="document.querySelector('[data-page=services]').click()" style="background:none;border:none;font-family:Poppins,sans-serif;font-size:0.78rem;font-weight:600;color:#2563eb;cursor:pointer;">View All →</button>      </div>      <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:16px;">        ${feedJobs.map(job => {          const budgetText = (job.budgetMin || job.budgetMax)            ? `₱${job.budgetMin || 0} – ₱${job.budgetMax || 0}` : 'Budget not set';          const postedDate = job.postedAt ? new Date(job.postedAt).toLocaleDateString() : '';          return `            <div style="background:#fff;border-radius:14px;border:1.5px solid #e8eff6;padding:20px;font-family:Poppins,sans-serif;transition:all 0.2s;cursor:pointer;"                  onmouseover="this.style.borderColor='var(--cyan)';this.style.boxShadow='0 4px 20px rgba(0,200,240,0.12)'"                  onmouseout="this.style.borderColor='#e8eff6';this.style.boxShadow='none'">              <div style="display:flex;align-items:center;gap:10px;margin-bottom:10px;">                <span style="font-size:1.5rem;">${(catName(job.category).split(' ')[0]) || '💼'}</span>                <span class="dash-badge dash-badge--green" style="font-size:0.68rem;">Open</span>              </div>              <div style="font-size:0.92rem;font-weight:700;color:#1a2a3a;margin-bottom:4px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${job.title}</div>              <div style="font-size:0.75rem;color:#7a8fa0;margin-bottom:8px;">${catName(job.category)}${job.subcategory ? ' › ' + job.subcategory : ''}</div>              <div style="font-size:0.78rem;color:#5a6a7a;margin-bottom:10px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${job.description}</div>              <div style="display:flex;align-items:center;justify-content:space-between;">                <span style="font-size:0.82rem;font-weight:700;color:#1b3d7a;">${budgetText}</span>                <span style="font-size:0.7rem;color:#aab5c0;">${postedDate}</span>              </div>              <div style="font-size:0.72rem;color:#8a9baa;margin-top:6px;">Posted by: <strong>${job.postedBy}</strong></div>              ${job.duration ? `<div style="font-size:0.7rem;color:#8a9baa;margin-top:2px;">⏱ ${job.duration}</div>` : ''}              <button onclick="event.stopPropagation();feedAcceptJob('${job.id}')" style="width:100%;margin-top:12px;padding:8px;font-size:0.78rem;font-weight:700;font-family:Poppins,sans-serif;border:none;background:var(--cyan);color:#fff;border-radius:8px;cursor:pointer;box-shadow:0 2px 10px rgba(0,200,240,0.3);transition:all 0.2s;" onmouseover="this.style.opacity='0.88'" onmouseout="this.style.opacity='1'">✋ Accept Job</button>            </div>`;        }).join('')}      </div>`;  }  async function renderClientFeed(container) {    const res = await fetch(`${API_BASE}/feed/client/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) throw new Error(data.message);    const freelancers = data.freelancers || [];    if (freelancers.length === 0) {      container.innerHTML = `        <div style="text-align:center;padding:50px 20px;">          <div style="font-size:2.5rem;margin-bottom:12px;">👤</div>          <div style="font-size:1rem;font-weight:600;color:#1a2a3a;margin-bottom:6px;font-family:Poppins,sans-serif;">No freelancers to show yet</div>          <div style="font-size:0.82rem;color:#7a8fa0;font-family:Poppins,sans-serif;">Post a job to discover matching freelancers, or check back later!</div>        </div>`;      return;    }    container.innerHTML = `      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px;">        <h3 style="font-family:Poppins,sans-serif;font-size:1.05rem;font-weight:700;color:#c06010;margin:0;">🎯 Freelancers For You</h3>      </div>      <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:16px;">        ${freelancers.map(fl => {          const avatar = fl.avatarSrc            ? `<img src="${fl.avatarSrc}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`            : `<span style="font-size:1.3rem;">👤</span>`;          const skills = [fl.skill, fl.tools].filter(Boolean).join(' • ');          return `            <div style="background:#fff;border-radius:14px;border:1.5px solid #e8eff6;padding:20px;font-family:Poppins,sans-serif;transition:all 0.2s;cursor:pointer;"                 onmouseover="this.style.borderColor='#f08020';this.style.boxShadow='0 4px 20px rgba(240,128,32,0.12)'"                 onmouseout="this.style.borderColor='#e8eff6';this.style.boxShadow='none'"                 onclick="feedViewProfile('${fl.username}')">              <div style="display:flex;align-items:center;gap:12px;margin-bottom:12px;">                <div style="width:48px;height:48px;border-radius:50%;background:linear-gradient(135deg,#fde8d0,#fbd5a8);display:flex;align-items:center;justify-content:center;overflow:hidden;flex-shrink:0;">                  ${avatar}                </div>                <div style="min-width:0;">                  <div style="font-size:0.9rem;font-weight:700;color:#1a2a3a;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${fl.name}</div>                  <div style="font-size:0.75rem;color:#7a8fa0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${fl.title}</div>                </div>              </div>              ${fl.bio ? `<div style="font-size:0.78rem;color:#5a6a7a;margin-bottom:10px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${fl.bio}</div>` : ''}              ${skills ? `<div style="font-size:0.72rem;color:#1b3d7a;margin-bottom:6px;">🛠 ${skills}</div>` : ''}              <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;">                ${fl.level ? `<span style="font-size:0.7rem;background:#e8f4fd;color:#1b3d7a;padding:3px 8px;border-radius:6px;font-weight:600;">${fl.level}</span>` : ''}                ${fl.years ? `<span style="font-size:0.7rem;color:#8a9baa;">${fl.years} exp</span>` : ''}                ${fl.hourly ? `<span style="font-size:0.7rem;font-weight:700;color:#16a34a;">₱${fl.hourly}/hr</span>` : ''}                ${fl.location ? `<span style="font-size:0.7rem;color:#8a9baa;">📍 ${fl.location}</span>` : ''}              </div>              <div style="display:flex;gap:8px;margin-top:12px;">                <button onclick="event.stopPropagation();feedMessageUser('${fl.username}')" style="flex:1;padding:8px;font-size:0.76rem;font-weight:600;font-family:Poppins,sans-serif;border:1.5px solid #f08020;background:#fff;color:#f08020;border-radius:8px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.background='#f08020';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#f08020'">💬 Message</button>              </div>            </div>`;        }).join('')}      </div>`;  }  async function feedAcceptJob(jobId) {    if (!confirm('Accept this job?')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'PATCH',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ status: 'in-progress', acceptedBy: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed');      showToast('Job accepted!', 'success');      loadDashboardFeed();    } catch (err) {      showToast('Error: ' + err.message, 'error');    }  }  function feedMessageUser(username) {    // Switch to messages page and open chat with this user    chatPartner = username;    document.querySelector('[data-page=messages]').click();    setTimeout(() => openChat(username), 300);  }  function feedViewProfile(username) {    // Open a lightweight profile preview via message (simplest approach)    feedMessageUser(username);  }  /* ══════════════════════════════════════════     HELPER FUNCTIONS     ══════════════════════════════════════════ */  /* Show a toast notification */  function showToast(message, type) {    let toast = document.getElementById('gighubToast');    if (!toast) {      toast = document.createElement('div');      toast.id = 'gighubToast';      toast.style.cssText = 'position:fixed;top:24px;right:24px;z-index:99999;padding:14px 28px;border-radius:12px;font-family:Poppins,sans-serif;font-size:0.88rem;font-weight:600;color:#fff;box-shadow:0 4px 24px rgba(0,0,0,0.18);transform:translateY(-20px);opacity:0;transition:all 0.35s ease;pointer-events:none;max-width:400px;';      document.body.appendChild(toast);    }    toast.textContent = message;    if (type === 'success') toast.style.background = 'linear-gradient(135deg,#16a34a,#22c55e)';    else if (type === 'warning') toast.style.background = 'linear-gradient(135deg,#ea580c,#f59e0b)';    else toast.style.background = 'linear-gradient(135deg,#ef4444,#f87171)';    toast.style.opacity = '1';    toast.style.transform = 'translateY(0)';    clearTimeout(toast._timer);    toast._timer = setTimeout(() => {      toast.style.opacity = '0';      toast.style.transform = 'translateY(-20px)';    }, 3000);  }  /* Replace a display element with an editable input */  function replaceWithInput(displayId, editId, value, fontSize, fontWeight, isTextarea) {    const display = document.getElementById(displayId);    if (!display) return;    display.style.display = 'none';    const wrap = document.createElement('div');    wrap.id = editId + '-wrap';    let el;    if (isTextarea) {      el = document.createElement('textarea');      el.rows = 3;      el.style.resize = 'vertical';    } else {      el = document.createElement('input');      el.type = 'text';    }    el.id = editId;    el.value = value || '';    el.style.cssText = `width:100%;font-size:${fontSize};font-weight:${fontWeight};font-family:Poppins,sans-serif;border:1.5px solid #1b3d7a;border-radius:8px;padding:8px 12px;outline:none;background:#f7fbff;box-shadow:0 0 0 2px rgba(27,61,122,0.08);box-sizing:border-box;`;    wrap.appendChild(el);    display.parentNode.insertBefore(wrap, display.nextSibling);  }  /* Get the value from an edit input, falling back to the display element */  function getEditVal(editId, displayId) {    const el = document.getElementById(editId);    if (el) return el.value;    const display = document.getElementById(displayId);    return display ? display.textContent : '';  }  /* Star picker click handler */  function pickStar(n) {    selectedStarRating = n;    updateStarPicker(n);  }  /* ══════════════════════════════════════════     PROFILE — render & edit (role-independent)     ══════════════════════════════════════════ */  function renderProfile() {    const p = profiles[currentRole];    exitProfileEdit(); // always start in view mode    /* Hero */    document.getElementById('profileNameDisplay').textContent  = p.name;    document.getElementById('profileTitleDisplay').textContent = p.title;    document.getElementById('profileBioDisplay').textContent   = p.bio;    document.getElementById('profileMetaDisplay').innerHTML    = p.languages + '<br>' + p.location;    /* Avatar */    const av = document.getElementById('profileAvatarDisplay');    if (p.avatarSrc) {      av.innerHTML = `<img src="${p.avatarSrc}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`;    } else {      av.innerHTML = '👤';    }    /* Skills */    document.getElementById('pfSkill').value       = p.skill;    document.getElementById('pfTools').value       = p.tools;    document.getElementById('pfLevel').value       = p.level;    document.getElementById('pfYears').value       = p.years;    /* Rates */    document.getElementById('pfHourly').value      = p.hourly;    document.getElementById('pfFixedMin').value    = p.fixedMin;    document.getElementById('pfFixedMax').value    = p.fixedMax;    document.getElementById('pfPayment').value     = p.payment;    /* Education */    document.getElementById('pfDegree').value      = p.degree;    document.getElementById('pfInstitution').value = p.institution;    document.getElementById('pfGradYear').value    = p.gradYear;    document.getElementById('pfCerts').value       = p.certs;    /* Portfolio */    updatePortfolioUI(p);    /* Load real reviews */    loadProfileReviews();    /* Load real activity counters */    loadActivityCounters();  }  async function loadProfileReviews() {    try {      const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(currentUser)}`);      if (!res.ok) {        console.error('Reviews endpoint returned', res.status);        return;      }      const text = await res.text();      let data;      try { data = JSON.parse(text); } catch (e) {        console.error('Failed to parse reviews response:', text);        return;      }      const reviews = data.success ? (data.reviews || []) : [];      const badge = document.getElementById('profileAvgBadge');      const listEl = document.getElementById('profileReviewsList');      if (reviews.length === 0) {        badge.textContent = '⭐ —';        listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:20px 0;font-family:Poppins,sans-serif;">No reviews yet. Reviews from other users will appear here.</div>';        return;      }      const avg = reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length;      badge.textContent = `⭐ ${avg.toFixed(1)}`;      listEl.innerHTML = reviews.map(r => {        const initial = r.from.charAt(0).toUpperCase();        const stars = renderStarString(r.rating);        return `          <div class="review-item">            <div class="review-avatar">${initial}</div>            <div>              <div style="font-size:0.78rem;font-weight:700;color:#1a2a3a;margin-bottom:2px;">${r.from}</div>              <div class="review-text">${r.text || 'No comment.'}</div>              <div class="review-stars">${stars}</div>            </div>          </div>`;      }).join('');    } catch (err) {      console.error('Error loading profile reviews:', err);    }  }  /* Toggle edit / save */  function toggleProfileEdit() {    if (isEditingProfile) {      saveProfileEdit();    } else {      enterProfileEdit();    }  }  function enterProfileEdit() {    isEditingProfile = true;    const btn = document.getElementById('editProfileBtn');    btn.innerHTML = `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg> Save`;    /* Make all profile fields editable + add visual edit indicator */    const fields = ['pfSkill','pfTools','pfLevel','pfYears','pfHourly','pfFixedMin','pfFixedMax','pfPayment','pfDegree','pfInstitution','pfGradYear','pfCerts'];    fields.forEach(id => {      const el = document.getElementById(id);      if (el) {        el.removeAttribute('readonly');        el.style.background = '#f7fbff';        el.style.border = '1.5px solid #1b3d7a';        el.style.boxShadow = '0 0 0 2px rgba(27,61,122,0.08)';        el.style.cursor = 'text';        if (!el.placeholder || el.placeholder === '') el.placeholder = 'Click to edit…';      }    });    /* Swap hero display elements to inputs */    replaceWithInput('profileNameDisplay',  'pf-edit-name',  profiles[currentRole].name,      '1.1rem','700');    replaceWithInput('profileTitleDisplay', 'pf-edit-title', profiles[currentRole].title,     '0.8rem','400');    replaceWithInput('profileBioDisplay',   'pf-edit-bio',   profiles[currentRole].bio,       '0.84rem','400', true);    replaceWithInput('profileMetaDisplay',  'pf-edit-meta',  profiles[currentRole].languages + '\n' + profiles[currentRole].location, '0.8rem','400', true);    /* Show edit mode banner */    let banner = document.getElementById('editModeBanner');    if (!banner) {      banner = document.createElement('div');      banner.id = 'editModeBanner';      banner.style.cssText = 'background:linear-gradient(135deg,#1b3d7a,#2563eb);color:#fff;padding:10px 20px;border-radius:10px;font-family:Poppins,sans-serif;font-size:0.82rem;font-weight:600;text-align:center;margin-bottom:16px;display:flex;align-items:center;justify-content:center;gap:8px;';      banner.innerHTML = '✏️ <span>Edit Mode — All highlighted fields are editable. Click Save when done.</span>';      const profilePage = document.getElementById('profilePage');      if (profilePage) profilePage.insertBefore(banner, profilePage.firstChild);    }    banner.style.display = 'flex';  }  function saveProfileEdit() {    const p = profiles[currentRole];    /* Hero fields */    const name      = getEditVal('pf-edit-name',  'profileNameDisplay').trim();    const title     = getEditVal('pf-edit-title', 'profileTitleDisplay').trim();    const bio       = getEditVal('pf-edit-bio',   'profileBioDisplay').trim();    const metaRaw   = getEditVal('pf-edit-meta', 'profileMetaDisplay').trim();    const metaLines = metaRaw.split('\n');    const languages = metaLines[0]?.trim() || '';    const location  = metaLines[1]?.trim() || '';    /* Fields */    const skill       = document.getElementById('pfSkill').value.trim();    const tools       = document.getElementById('pfTools').value.trim();    const level       = document.getElementById('pfLevel').value.trim();    const years       = document.getElementById('pfYears').value.trim();    const hourly      = document.getElementById('pfHourly').value.trim();    const fixedMin    = document.getElementById('pfFixedMin').value.trim();    const fixedMax    = document.getElementById('pfFixedMax').value.trim();    const payment     = document.getElementById('pfPayment').value.trim();    const degree      = document.getElementById('pfDegree').value.trim();    const institution = document.getElementById('pfInstitution').value.trim();    const gradYear    = document.getElementById('pfGradYear').value.trim();    const certs       = document.getElementById('pfCerts').value.trim();    /* Only validate the essentials — allow partial saves */    if (!name) {      alert('Name is required.');      return;    }    /* Save all data */    p.name       = name;    p.title      = title || '';    p.bio        = bio || '';    p.languages  = languages || '';    p.location   = location || '';    p.skill      = skill || '';    p.tools      = tools || '';    p.level      = level || '';    p.years      = years || '';    p.hourly     = hourly || '';    p.fixedMin   = fixedMin || '';    p.fixedMax   = fixedMax || '';    p.payment    = payment || '';    p.degree     = degree || '';    p.institution = institution || '';    p.gradYear   = gradYear || '';    p.certs      = certs || '';    /* Sync to database (fire and forget — no await needed) */    syncProfileToDatabase().then(saved => {      if (saved) {        showToast('✅ Profile saved successfully!', 'success');      } else {        showToast('⚠️ Profile saved locally but failed to sync to server.', 'warning');      }    });    exitProfileEdit();    renderProfile();  }  function exitProfileEdit() {    isEditingProfile = false;    const btn = document.getElementById('editProfileBtn');    if (btn) btn.innerHTML = `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg> Edit`;    const fields = ['pfSkill','pfTools','pfLevel','pfYears','pfHourly','pfFixedMin','pfFixedMax','pfPayment','pfDegree','pfInstitution','pfGradYear','pfCerts'];    fields.forEach(id => {      const el = document.getElementById(id);      if (el) {        el.setAttribute('readonly', '');        el.style.background = '';        el.style.border = '';        el.style.boxShadow = '';        el.style.cursor = '';      }    });    /* Remove edit wrappers */    ['pf-edit-name-wrap','pf-edit-title-wrap','pf-edit-bio-wrap','pf-edit-meta-wrap'].forEach(wid => {      const wrap = document.getElementById(wid);      if (wrap) wrap.remove();    });    /* Restore display elements */    ['profileNameDisplay','profileTitleDisplay','profileBioDisplay','profileMetaDisplay'].forEach(did => {      const el = document.getElementById(did);      if (el) el.style.display = '';    });    /* Hide edit banner */    const banner = document.getElementById('editModeBanner');    if (banner) banner.style.display = 'none';  }  /* ── Avatar upload ───────────────────────── */  function handleAvatarChange(event) {    const file = event.target.files[0];    if (!file) return;    const reader = new FileReader();    reader.onload = e => {      profiles[currentRole].avatarSrc = e.target.result;      const av = document.getElementById('profileAvatarDisplay');      av.innerHTML = `<img src="${e.target.result}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`;    };    reader.readAsDataURL(file);  }  /* ══════════════════════════════════════════     PORTFOLIO PDF     ══════════════════════════════════════════ */  function handlePortfolioUpload(event) {    const file = event.target.files[0];    if (!file || file.type !== 'application/pdf') return;    if (portfolioObjectUrl) URL.revokeObjectURL(portfolioObjectUrl);    portfolioObjectUrl = URL.createObjectURL(file);    profiles[currentRole].portfolioFile     = portfolioObjectUrl;    profiles[currentRole].portfolioFileName = file.name;    updatePortfolioUI(profiles[currentRole]);  }  function updatePortfolioUI(p) {    const empty = document.getElementById('portfolioEmpty');    const fileEl = document.getElementById('portfolioFile');    if (p.portfolioFile) {      empty.style.display  = 'none';      fileEl.style.display = '';      document.getElementById('portfolioFileName').textContent = p.portfolioFileName;    } else {      empty.style.display  = '';      fileEl.style.display = 'none';    }  }  function removePortfolio() {    if (portfolioObjectUrl) { URL.revokeObjectURL(portfolioObjectUrl); portfolioObjectUrl = null; }    profiles[currentRole].portfolioFile     = null;    profiles[currentRole].portfolioFileName = null;    document.getElementById('portfolioPdfInput').value = '';    updatePortfolioUI(profiles[currentRole]);  }  function viewPortfolioPdf() {    const url = profiles[currentRole].portfolioFile;    if (url) window.open(url, '_blank');  }  function togglePortfolioView() {    const url = profiles[currentRole].portfolioFile;    if (url) {      window.open(url, '_blank');    } else {      document.getElementById('portfolioPdfInput').click();    }  }  /* ══════════════════════════════════════════     DATABASE SYNC (Backend Only)     User profiles automatically sync to JSON     ══════════════════════════════════════════ */  async function syncProfileToDatabase() {    const profileData = { ...profiles[currentRole] };    try {      const res = await fetch(`${API_BASE}/profiles/save`, {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({          username: currentUser,          role: currentRole,          profile: profileData        })      });      const data = await res.json();      if (!res.ok || !data.success) {        console.error('Failed to sync profile:', data.message);        return false;      }      console.log('✅ Profile synced for role:', currentRole);      if (data.user) account = data.user;      return true;    } catch (err) {      console.error('Error syncing profile:', err);      return false;    }  }    /* Production API call to backend:       fetch('/api/profiles/save', {         method: 'POST',         headers: { 'Content-Type': 'application/json' },         body: JSON.stringify(profileData)       }).then(res => res.json())         .then(data => {           if (data.success) console.log('Profile synced to database');         });    */  /* ══════════════════════════════════════════     JOBS MANAGEMENT     ══════════════════════════════════════════ */  let activeJobFilter = 'all';  // Load jobs based on current role (CLIENT or FREELANCER)  async function loadJobsForRole() {    try {      const isClient = currentRole === 'client';      const endpoint = isClient        ? `${API_BASE}/jobs/my/${encodeURIComponent(currentUser)}`        : `${API_BASE}/jobs/browse/${encodeURIComponent(currentUser)}`;      const res = await fetch(endpoint);      const data = await res.json();      if (!data.success) throw new Error(data.message || 'Failed to load jobs.');      jobs = data.jobs || [];      // Cache full browse results for client-side filtering      if (!isClient) allBrowseJobs = [...jobs];      // Also load categories      const catRes = await fetch(`${API_BASE}/categories`);      const catData = await catRes.json();      if (catData.success) categories = catData.categories || [];      console.log(`✅ Loaded ${jobs.length} jobs for ${currentRole} (${currentUser}):`, jobs);      renderJobs(activeJobFilter);    } catch (err) {      console.error('Error loading jobs:', err);      jobs = [];      renderJobs(activeJobFilter);    }  }  function getCategoryName(catId) {    const cat = categories.find(c => c.id === catId);    return cat ? `${cat.icon} ${cat.name}` : catId;  }  function populateCategoryDropdown() {    const select = document.getElementById('jobCategory');    if (!select || !categories.length) return;    select.innerHTML = '<option value="">Select Category</option>';    categories.forEach(cat => {      const option = document.createElement('option');      option.value = cat.id;      option.textContent = `${cat.icon} ${cat.name}`;      select.appendChild(option);    });    select.addEventListener('change', updateSubcategoryDropdown);  }  function updateSubcategoryDropdown() {    const catId = document.getElementById('jobCategory')?.value;    const subcatSelect = document.getElementById('jobSubcategory');    if (!subcatSelect) return;    subcatSelect.innerHTML = '<option value="">Select Subcategory</option>';    if (!catId) {      subcatSelect.style.display = 'none';      return;    }    const cat = categories.find(c => c.id === catId);    if (cat && cat.subcategories) {      cat.subcategories.forEach(sub => {        const opt = document.createElement('option');        opt.value = sub;        opt.textContent = sub;        subcatSelect.appendChild(opt);      });      subcatSelect.style.display = '';    } else {      subcatSelect.style.display = 'none';    }  }  function filterJobs(filter, btn) {    activeJobFilter = filter;    document.querySelectorAll('.services-tab').forEach(t => t.classList.remove('active'));    btn.classList.add('active');    renderJobs(filter);  }  function renderJobs(filter) {    const grid = document.getElementById('jobsGrid');    if (!grid) {      console.warn('jobsGrid element not found');      return;    }    let filtered = [...(jobs || [])];    const isClient = currentRole === 'client';    console.log(`🎨 renderJobs called: filter="${filter}", total jobs=${filtered.length}, role=${currentRole}, user=${currentUser}`);    // Filter by status    if (filter && filter !== 'all') {      filtered = filtered.filter(job => job.status === filter);    }    console.log(`✂️ After status filtering (${filter}): ${filtered.length} jobs match`);    if (filtered.length === 0) {      grid.innerHTML = `        <div class="services-empty" style="grid-column:1/-1;text-align:center;padding:60px 20px;">          <div class="services-empty__icon">${isClient ? '📋' : '🔍'}</div>          <div class="services-empty__text" style="color:#7a8fa0;">            ${isClient ? 'You haven\'t posted any jobs yet.' : 'No available jobs to browse right now.'}          </div>        </div>`;      return;    }    grid.innerHTML = '';    filtered.forEach(job => {      const card = document.createElement('div');      card.className = 'service-card';      card.style.cursor = 'pointer';      // Status badge color      let badgeClass = 'dash-badge--blue';      let statusLabel = job.status;      if (job.status === 'open') { badgeClass = 'dash-badge--green'; statusLabel = 'Open'; }      else if (job.status === 'in-progress') { badgeClass = 'dash-badge--yellow'; statusLabel = 'In Progress'; }      else if (job.status === 'completed') { badgeClass = 'dash-badge--blue'; statusLabel = 'Completed'; }      else if (job.status === 'cancelled') { badgeClass = 'dash-badge--red'; statusLabel = 'Cancelled'; }      const budgetText = (job.budgetMin || job.budgetMax)        ? `₱${job.budgetMin || 0} – ₱${job.budgetMax || 0}` : 'Budget not set';      const categoryLabel = getCategoryName(job.category);      const subcatLabel = job.subcategory ? ` › ${job.subcategory}` : '';      const postedDate = job.postedAt ? new Date(job.postedAt).toLocaleDateString() : '';      const acceptedLabel = job.acceptedBy ? `<div style="font-size:0.75rem;color:#16a34a;margin-top:4px;">Accepted by: <strong>${job.acceptedBy}</strong></div>` : '';      // Action buttons      let actionButtons = '';      if (isClient) {        // Client sees Edit & Delete on their own jobs        actionButtons = `          <div style="display:flex;gap:8px;margin-top:10px;">            <button onclick="event.stopPropagation();openEditJob('${job.id}')" style="flex:1;padding:6px 12px;font-size:0.75rem;font-weight:600;font-family:Poppins,sans-serif;border:1.5px solid #1b3d7a;background:#fff;color:#1b3d7a;border-radius:8px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.background='#1b3d7a';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#1b3d7a'">✏️ Edit</button>            <button onclick="event.stopPropagation();deleteJob('${job.id}')" style="flex:1;padding:6px 12px;font-size:0.75rem;font-weight:600;font-family:Poppins,sans-serif;border:1.5px solid #ef4444;background:#fff;color:#ef4444;border-radius:8px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.background='#ef4444';this.style.color='#fff'" onmouseout="this.style.background='#fff';this.style.color='#ef4444'">🗑 Delete</button>          </div>`;      } else {        // Freelancer sees Accept button (only on open jobs not yet accepted)        if (job.status === 'open' && !job.acceptedBy) {          actionButtons = `            <div style="margin-top:10px;">              <button onclick="event.stopPropagation();acceptJobFromCard('${job.id}')" style="width:100%;padding:8px 12px;font-size:0.78rem;font-weight:700;font-family:Poppins,sans-serif;border:none;background:var(--cyan);color:#fff;border-radius:8px;cursor:pointer;box-shadow:0 2px 10px rgba(0,200,240,0.3);transition:all 0.2s;" onmouseover="this.style.background='#00afd4'" onmouseout="this.style.background='var(--cyan)'">✋ Accept Job</button>            </div>`;        } else if (job.status === 'in-progress' && job.acceptedBy === currentUser) {          actionButtons = `            <div style="margin-top:10px;">              <button onclick="event.stopPropagation();completeJobFromCard('${job.id}')" style="width:100%;padding:8px 12px;font-size:0.78rem;font-weight:700;font-family:Poppins,sans-serif;border:none;background:linear-gradient(135deg,#16a34a,#22c55e);color:#fff;border-radius:8px;cursor:pointer;box-shadow:0 2px 10px rgba(22,163,74,0.3);transition:all 0.2s;" onmouseover="this.style.opacity='0.88'" onmouseout="this.style.opacity='1'">✅ Mark as Complete</button>            </div>`;        }      }      card.innerHTML = `        <div class="service-card__img" style="display:flex;align-items:center;justify-content:center;font-size:2rem;background:linear-gradient(135deg,#e8f4fd,#d0eafb);">          ${categoryLabel.split(' ')[0] || '💼'}        </div>        <div class="service-card__body">          <div class="service-card__title" style="font-size:0.9rem;font-weight:700;color:#1a2a3a;margin-bottom:4px;">${job.title}</div>          <div style="font-size:0.75rem;color:#7a8fa0;margin-bottom:6px;">${categoryLabel}${subcatLabel}</div>          <div style="font-size:0.78rem;color:#5a6a7a;margin-bottom:6px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${job.description}</div>          <div class="service-card__price" style="font-size:0.85rem;font-weight:700;margin-bottom:4px;">${budgetText}</div>          ${job.duration ? `<div style="font-size:0.72rem;color:#8a9baa;">⏱ ${job.duration}</div>` : ''}          <div style="display:flex;align-items:center;justify-content:space-between;margin-top:8px;">            <span class="dash-badge ${badgeClass}" style="font-size:0.7rem;">${statusLabel}</span>            <span style="font-size:0.7rem;color:#aab5c0;">${postedDate}</span>          </div>          ${isClient ? '' : `<div style="font-size:0.72rem;color:#8a9baa;margin-top:4px;">Posted by: <strong>${job.postedBy}</strong></div>`}          ${acceptedLabel}          ${actionButtons}        </div>`;      grid.appendChild(card);    });  }  function openPostJob() {    // Reset form fields    resetJobForm();    document.getElementById('postJobOverlay').classList.add('active');  }  function closePostJob() {    document.getElementById('postJobOverlay').classList.remove('active');    resetJobForm();  }  let allBrowseJobs = []; // unfiltered browse results cache  function openBrowseJobs() {    if (!isBrowsingAvailableJobs) {      isBrowsingAvailableJobs = true;      activeJobFilter = 'all';      populateBrowseCategoryFilter();    }    loadJobsForRole();  }  function populateBrowseCategoryFilter() {    const sel = document.getElementById('browseCategoryFilter');    if (!sel) return;    sel.innerHTML = '<option value="">All Categories</option>';    categories.forEach(cat => {      const opt = document.createElement('option');      opt.value = cat.id;      opt.textContent = `${cat.icon} ${cat.name}`;      sel.appendChild(opt);    });  }  function updateBrowseSubcategoryFilter() {    const catId = document.getElementById('browseCategoryFilter')?.value;    const subcatSel = document.getElementById('browseSubcategoryFilter');    if (!subcatSel) return;    subcatSel.innerHTML = '<option value="">All Subcategories</option>';    if (!catId) {      subcatSel.style.display = 'none';      return;    }    const cat = categories.find(c => c.id === catId);    if (cat && cat.subcategories) {      cat.subcategories.forEach(sub => {        const opt = document.createElement('option');        opt.value = sub;        opt.textContent = sub;        subcatSel.appendChild(opt);      });      subcatSel.style.display = '';    } else {      subcatSel.style.display = 'none';    }  }  function applyBrowseFilters() {    // Update subcategory dropdown when category changes    updateBrowseSubcategoryFilter();    const keyword = (document.getElementById('browseKeyword')?.value || '').trim().toLowerCase();    const catFilter = document.getElementById('browseCategoryFilter')?.value || '';    const subcatFilter = document.getElementById('browseSubcategoryFilter')?.value || '';    let filtered = [...allBrowseJobs];    if (catFilter) {      filtered = filtered.filter(j => j.category === catFilter);    }    if (subcatFilter) {      filtered = filtered.filter(j => j.subcategory === subcatFilter);    }    if (keyword) {      filtered = filtered.filter(j => {        const haystack = `${j.title} ${j.description} ${getCategoryName(j.category)} ${j.subcategory || ''}`.toLowerCase();        return keyword.split(/\s+/).every(w => haystack.includes(w));      });    }    jobs = filtered;    renderJobs(activeJobFilter);  }  function clearBrowseFilters() {    const kw = document.getElementById('browseKeyword');    const cat = document.getElementById('browseCategoryFilter');    const subcat = document.getElementById('browseSubcategoryFilter');    if (kw) kw.value = '';    if (cat) cat.value = '';    if (subcat) { subcat.value = ''; subcat.style.display = 'none'; }    // Restore full list    if (isBrowsingAvailableJobs && allBrowseJobs.length) {      jobs = [...allBrowseJobs];      renderJobs(activeJobFilter);    }  }  function resetJobForm() {    const fields = ['jobTitle', 'jobDescription', 'jobCategory', 'jobSubcategory', 'jobBudgetMin', 'jobBudgetMax', 'jobDuration', 'jobAttachment'];    fields.forEach(id => {      const el = document.getElementById(id);      if (el) el.value = '';    });  }  async function submitJob() {    const title = document.getElementById('jobTitle')?.value.trim();    const description = document.getElementById('jobDescription')?.value.trim();    const category = document.getElementById('jobCategory')?.value;    const subcategory = document.getElementById('jobSubcategory')?.value || '';    const budgetMin = parseFloat(document.getElementById('jobBudgetMin')?.value) || 0;    const budgetMax = parseFloat(document.getElementById('jobBudgetMax')?.value) || 0;    const duration = document.getElementById('jobDuration')?.value.trim() || '';    const attachmentName = document.getElementById('jobAttachment')?.value.trim() || '';    if (!title || !description || !category) {      alert('Please fill in the job title, description, and category.');      return;    }    try {      const res = await fetch(`${API_BASE}/jobs`, {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({          username: currentUser,          title, description, category, subcategory,          budgetMin, budgetMax, duration, attachmentName        })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to post job.');      alert('Job posted successfully!');      closePostJob();      await loadJobsForRole();    } catch (err) {      alert('Error posting job: ' + err.message);    }  }  async function acceptJobFromCard(jobId) {    if (!confirm('Are you sure you want to accept this job?')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'PATCH',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ status: 'in-progress', acceptedBy: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to accept job.');      showToast('Job accepted!', 'success');      await loadJobsForRole();      loadNotifications_UI();      loadActivityCounters();    } catch (err) {      alert('Error: ' + err.message);    }  }  async function completeJobFromCard(jobId) {    if (!confirm('Mark this job as completed? The client will be notified.')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'PATCH',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ status: 'completed', completedBy: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to complete job.');      showToast('Job marked as completed!', 'success');      await loadJobsForRole();      loadNotifications_UI();      loadActivityCounters();    } catch (err) {      alert('Error: ' + err.message);    }  }  // Edit job (for clients only)  let editingJobId = null;  function openEditJob(jobId) {    const job = jobs.find(j => j.id === jobId);    if (!job) return;    editingJobId = jobId;    const editTitle = document.getElementById('editJobTitle');    const editDesc = document.getElementById('editJobDescription');    const editCat = document.getElementById('editJobCategory');    const editSubcat = document.getElementById('editJobSubcategory');    const editBudgetMin = document.getElementById('editJobBudgetMin');    const editBudgetMax = document.getElementById('editJobBudgetMax');    const editDuration = document.getElementById('editJobDuration');    const editStatus = document.getElementById('editJobStatus');    if (editTitle) editTitle.value = job.title || '';    if (editDesc) editDesc.value = job.description || '';    if (editCat) {      editCat.innerHTML = '<option value="">Select Category</option>';      categories.forEach(cat => {        const opt = document.createElement('option');        opt.value = cat.id;        opt.textContent = `${cat.icon} ${cat.name}`;        if (cat.id === job.category) opt.selected = true;        editCat.appendChild(opt);      });      // Populate subcategory for current category      updateEditSubcategoryDropdown(job.subcategory);      editCat.onchange = () => updateEditSubcategoryDropdown('');    }    if (editBudgetMin) editBudgetMin.value = job.budgetMin || '';    if (editBudgetMax) editBudgetMax.value = job.budgetMax || '';    if (editDuration) editDuration.value = job.duration || '';    if (editStatus) editStatus.value = job.status || 'open';    document.getElementById('editJobOverlay').classList.add('active');  }  function updateEditSubcategoryDropdown(preselect) {    const catSelect = document.getElementById('editJobCategory');    const subcatSelect = document.getElementById('editJobSubcategory');    if (!catSelect || !subcatSelect) return;    const selectedCat = categories.find(c => c.id === catSelect.value);    subcatSelect.innerHTML = '<option value="">Select Subcategory</option>';    if (selectedCat && selectedCat.subcategories) {      selectedCat.subcategories.forEach(sub => {        const opt = document.createElement('option');        opt.value = sub;        opt.textContent = sub;        if (sub === preselect) opt.selected = true;        subcatSelect.appendChild(opt);      });    }  }  function closeEditJob() {    document.getElementById('editJobOverlay').classList.remove('active');    editingJobId = null;  }  async function saveEditJob() {    if (!editingJobId) return;    const title = document.getElementById('editJobTitle')?.value.trim();    const description = document.getElementById('editJobDescription')?.value.trim();    const category = document.getElementById('editJobCategory')?.value;    const subcategory = document.getElementById('editJobSubcategory')?.value || '';    const budgetMin = parseFloat(document.getElementById('editJobBudgetMin')?.value) || 0;    const budgetMax = parseFloat(document.getElementById('editJobBudgetMax')?.value) || 0;    const duration = document.getElementById('editJobDuration')?.value.trim() || '';    const status = document.getElementById('editJobStatus')?.value || 'open';    if (!title || !description || !category) {      alert('Title, description, and category are required.');      return;    }    try {      const res = await fetch(`${API_BASE}/jobs/${editingJobId}`, {        method: 'PUT',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({          username: currentUser,          title, description, category, subcategory,          budgetMin, budgetMax, duration, status        })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to update job.');      alert('Job updated!');      closeEditJob();      await loadJobsForRole();    } catch (err) {      alert('Error: ' + err.message);    }  }  async function deleteJob(jobId) {    if (!confirm('Are you sure you want to delete this job? This cannot be undone.')) return;    try {      const res = await fetch(`${API_BASE}/jobs/${jobId}`, {        method: 'DELETE',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ username: currentUser })      });      const data = await res.json();      if (!res.ok || !data.success) throw new Error(data.message || 'Failed to delete job.');      alert('Job deleted.');      await loadJobsForRole();    } catch (err) {      alert('Error: ' + err.message);    }  }  async function loadFreelancerRecommendations(jobCategory) {    try {      const res = await fetch(`${API_BASE}/freelancers/by-category/${encodeURIComponent(jobCategory)}`);      const data = await res.json();      if (data.success) return data.freelancers || [];      return [];    } catch (err) {      console.error('Error loading recommendations:', err);      return [];    }  }  document.getElementById('postJobOverlay').addEventListener('click', function(e) {     if(e.target===this) closePostJob();   });  document.addEventListener('keydown', e => {    if (e.key === 'Escape') {      document.getElementById('roleModalOverlay').classList.remove('active');      document.getElementById('editJobOverlay').classList.remove('active');      document.getElementById('postJobOverlay').classList.remove('active');    }  });let chatPartner = '';let partnerProfileOpen = false;let selectedStarRating = 0;// Toggle partner profile sidebarfunction togglePartnerProfile() {  const panel = document.getElementById('partnerProfilePanel');  partnerProfileOpen = !partnerProfileOpen;  if (partnerProfileOpen && chatPartner) {    panel.style.width = '280px';    panel.style.borderLeft = '1.5px solid #e8eff6';    loadPartnerProfile(chatPartner);  } else {    panel.style.width = '0';    panel.style.borderLeft = '0';    partnerProfileOpen = false;  }}// Load partner's profile dataasync function loadPartnerProfile(username) {  try {    const panel = document.getElementById('partnerProfilePanel');    panel.style.width = '280px';    panel.style.borderLeft = '1.5px solid #e8eff6';    const res = await fetch(`${API_BASE}/users/${encodeURIComponent(username)}`);    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { console.error('Bad JSON from user endpoint:', text); return; }    const initial = username.charAt(0).toUpperCase();    document.getElementById('ppAvatar').textContent = initial;    document.getElementById('ppName').textContent = username;    if (data.success && data.user) {      const user = data.user;      const roleData = user.roles?.freelancer || user.roles?.client || {};      document.getElementById('ppTitle').textContent = roleData.title || 'GigHub User';      document.getElementById('ppSkill').textContent = roleData.skill || 'Not specified';      document.getElementById('ppTools').textContent = roleData.tools || 'Not specified';      document.getElementById('ppLevel').textContent = roleData.level || 'Not specified';      document.getElementById('ppYears').textContent = roleData.years ? (roleData.years + ' years') : 'Not specified';      if (roleData.avatarSrc) {        document.getElementById('ppAvatar').innerHTML = `<img src="${roleData.avatarSrc}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;" />`;      }    } else {      document.getElementById('ppTitle').textContent = 'GigHub User';      document.getElementById('ppSkill').textContent = 'Not specified';      document.getElementById('ppTools').textContent = 'Not specified';      document.getElementById('ppLevel').textContent = 'Not specified';      document.getElementById('ppYears').textContent = 'Not specified';    }    // Load reviews for this user    loadUserReviews(username);    // Reset star picker    selectedStarRating = 0;    updateStarPicker(0);    document.getElementById('ppReviewText').value = '';    // Check if current user already left a review    loadExistingReview(username);  } catch (err) {    console.error('Error loading partner profile:', err);  }}// Star pickerfunction updateStarPicker(n) {  document.querySelectorAll('.star-pick').forEach(star => {    const val = parseInt(star.dataset.star);    star.style.color = val <= n ? '#f5a623' : '#dce4ef';  });}// Hover effects for star pickerdocument.addEventListener('DOMContentLoaded', () => {  document.querySelectorAll('.star-pick').forEach(star => {    star.addEventListener('mouseenter', () => {      const hoverVal = parseInt(star.dataset.star);      document.querySelectorAll('.star-pick').forEach(s => {        s.style.color = parseInt(s.dataset.star) <= hoverVal ? '#f5a623' : '#dce4ef';      });    });    star.addEventListener('mouseleave', () => {      updateStarPicker(selectedStarRating);    });  });});// Submit a reviewasync function submitReview() {  if (!chatPartner) return;  if (selectedStarRating === 0) {    alert('Please select a star rating (1–5).');    return;  }  const reviewText = document.getElementById('ppReviewText').value.trim();  try {    const res = await fetch(`${API_BASE}/reviews`, {      method: 'POST',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({        from: currentUser,        to: chatPartner,        rating: selectedStarRating,        text: reviewText      })    });    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) {      console.error('Server response was not JSON:', text);      alert('Server error — check that your server is running and has the /api/reviews endpoint.');      return;    }    if (!res.ok || !data.success) throw new Error(data.message || 'Failed to submit review.');    showToast(data.message || '✅ Review submitted!', 'success');    loadUserReviews(chatPartner);    loadExistingReview(chatPartner);  } catch (err) {    alert('Error: ' + err.message);  }}// Load all reviews for a user (partner profile sidebar)async function loadUserReviews(username) {  try {    const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(username)}`);    if (!res.ok) { console.error('Reviews endpoint returned', res.status); return; }    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { return; }    const reviews = data.success ? (data.reviews || []) : [];    const avgContainer = document.getElementById('ppAvgRating');    const avgStars = document.getElementById('ppAvgStars');    const countEl = document.getElementById('ppRatingCount');    const listEl = document.getElementById('ppReviewsList');    if (!avgContainer || !listEl) return;    if (reviews.length === 0) {      avgContainer.textContent = '—';      avgStars.textContent = '☆☆☆☆☆';      countEl.textContent = '(0 reviews)';      listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.78rem;padding:16px 0;font-family:Poppins,sans-serif;">No reviews yet</div>';      return;    }    const avg = reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length;    avgContainer.textContent = avg.toFixed(1);    avgStars.textContent = renderStarString(Math.round(avg));    countEl.textContent = `(${reviews.length} review${reviews.length !== 1 ? 's' : ''})`;    listEl.innerHTML = reviews.map(r => {      const initial = r.from.charAt(0).toUpperCase();      const date = new Date(r.timestamp).toLocaleDateString();      const stars = renderStarString(r.rating);      return `        <div style="display:flex;gap:12px;padding:12px 0;border-bottom:1px solid #f0f4f8;">          <div style="width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.75rem;flex-shrink:0;">${initial}</div>          <div style="flex:1;">            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:3px;">              <div style="font-size:0.78rem;font-weight:700;color:#1a2a3a;">${r.from}</div>              <div style="font-size:0.62rem;color:#aab5c0;">${date}</div>            </div>            <div style="font-size:0.82rem;color:#f5a623;letter-spacing:1px;">${stars}</div>          </div>        </div>`;    }).join('');  } catch (err) {    console.error('Error loading reviews:', err);  }}// Check if current user already reviewed this personasync function loadExistingReview(username) {  try {    const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(username)}`);    if (!res.ok) return;    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { return; }    const reviews = data.success ? (data.reviews || []) : [];    const existing = reviews.find(r => r.from === currentUser);    if (existing) {      selectedStarRating = existing.rating;      updateStarPicker(existing.rating);      document.getElementById('ppReviewText').value = existing.text || '';    }  } catch (err) {    console.error('Error checking existing review:', err);  }}// Load profile reviews (for own profile page)async function loadProfileReviews() {  try {    const res = await fetch(`${API_BASE}/reviews/${encodeURIComponent(currentUser)}`);    if (!res.ok) return;    const text = await res.text();    let data;    try { data = JSON.parse(text); } catch(e) { return; }    const reviews = data.success ? (data.reviews || []) : [];    const badge = document.getElementById('profileAvgBadge');    const listEl = document.getElementById('profileReviewsList');    if (!badge || !listEl) return;    if (reviews.length === 0) {      badge.textContent = '⭐ —';      listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:20px 0;font-family:Poppins,sans-serif;">No reviews yet. Reviews from other users will appear here.</div>';      return;    }    const avg = reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length;    badge.textContent = `⭐ ${avg.toFixed(1)}`;    listEl.innerHTML = reviews.map(r => {      const initial = r.from.charAt(0).toUpperCase();      const stars = renderStarString(r.rating);      const date = new Date(r.timestamp).toLocaleDateString();      return `        <div style="display:flex;gap:12px;padding:12px 0;border-bottom:1px solid #f0f4f8;">          <div style="width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.75rem;flex-shrink:0;">${initial}</div>          <div style="flex:1;">            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:3px;">              <div style="font-size:0.78rem;font-weight:700;color:#1a2a3a;">${r.from}</div>              <div style="font-size:0.62rem;color:#aab5c0;">${date}</div>            </div>            <div style="font-size:0.82rem;color:#f5a623;letter-spacing:1px;margin-bottom:4px;">${stars}</div>            ${r.text ? `<div style="font-size:0.75rem;color:#4a5a6a;line-height:1.5;font-family:Poppins,sans-serif;">${r.text}</div>` : ''}          </div>        </div>`;    }).join('');  } catch (err) {    console.error('Error loading profile reviews:', err);  }}// Load real activity counters from the serverasync function loadActivityCounters() {  try {    const res = await fetch(`${API_BASE}/jobs/activity/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) return;    document.getElementById('actPosted').textContent = data.posted ?? 0;    document.getElementById('actCompleted').textContent = data.completed ?? 0;    document.getElementById('actOngoing').textContent = data.ongoing ?? 0;    // Update label based on role    const label = document.getElementById('actPostedLabel');    if (label) label.textContent = currentRole === 'client' ? 'Posted' : 'Accepted';  } catch (err) {    console.error('Error loading activity counters:', err);  }}// Render star stringfunction renderStarString(n) {  return '★'.repeat(Math.max(0, Math.min(5, n))) + '☆'.repeat(Math.max(0, 5 - Math.min(5, n)));}// Load inboxfunction loadInbox() {  fetch(`${API_BASE}/usernames?exclude=${encodeURIComponent(currentUser)}`)    .then(res => res.json())    .then(data => {      const inboxList = document.getElementById('inboxList');      const usernames = (data.usernames || []).filter(username => username !== currentUser);      if (!usernames.length) {        inboxList.innerHTML = '<div style="color:#aab5c0;font-size:0.85rem;padding:20px 8px;text-align:center;font-family:Poppins,sans-serif;">No users found.</div>';        return;      }      inboxList.innerHTML = usernames.map(u => {        const initial = u.charAt(0).toUpperCase();        return `<button onclick="openChat('${u}')" id="inbox-${u}" style="          display:flex;align-items:center;gap:12px;          text-align:left;padding:10px 14px;          border-radius:10px;border:none;          background:transparent;cursor:pointer;          width:100%;font-family:Poppins,sans-serif;          transition:background 0.15s;        " onmouseover="this.style.background='#e8eff6'" onmouseout="if(!this.classList.contains('inbox-active'))this.style.background='transparent'">          <div style="width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.65rem;flex-shrink:0;">${initial}</div>          <div style="overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:0.85rem;font-weight:600;color:#1a2a3a;">${u}</div>        </button>`;      }).join('');    });}// Open chat with a userfunction openChat(username) {  document.querySelectorAll('#inboxList button').forEach(btn => {    btn.classList.remove('inbox-active');    btn.style.background = 'transparent';  });  const activeBtn = document.getElementById('inbox-' + username);  if (activeBtn) {    activeBtn.classList.add('inbox-active');    activeBtn.style.background = '#e0eaf5';  }  chatPartner = username;  const initial = username.charAt(0).toUpperCase();  document.getElementById('chatPartnerName').textContent = username;  document.getElementById('chatPartnerStatus').textContent = 'Online';  document.getElementById('chatAvatarHeader').textContent = initial;  // Show the View Profile button  document.getElementById('viewPartnerProfileBtn').style.display = 'inline-flex';  // Close profile panel when switching users  if (partnerProfileOpen) {    const panel = document.getElementById('partnerProfilePanel');    panel.style.width = '0';    panel.style.borderLeft = '0';    partnerProfileOpen = false;  }  loadMessages();}// Load messagesfunction loadMessages() {  if (!chatPartner) return;  fetch(`${API_BASE}/messages?user1=${encodeURIComponent(currentUser)}`)    .then(res => res.json())    .then(data => {      const msgs = data.messages || [];      const box = document.getElementById('chatBox');      if (!msgs.length) {        box.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.85rem;margin:auto;font-family:Poppins,sans-serif;">No messages yet. Say hello! 👋</div>';        return;      }      box.innerHTML = msgs.map(m => {        const isMe = m.from === currentUser;        const time = new Date(m.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });        return `<div style="display:flex;justify-content:${isMe ? 'flex-end' : 'flex-start'};align-items:flex-end;gap:6px;">          ${!isMe ? `<div style="width:28px;height:28px;border-radius:50%;background:linear-gradient(135deg,#1b3d7a,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-size:0.65rem;font-weight:700;flex-shrink:0;">${m.from.charAt(0).toUpperCase()}</div>` : ''}          <div style="max-width:65%;display:flex;flex-direction:column;align-items:${isMe ? 'flex-end' : 'flex-start'};">            <div style="              padding:10px 16px;              border-radius:${isMe ? '18px 18px 4px 18px' : '18px 18px 18px 4px'};              background:${isMe ? 'linear-gradient(135deg,#1b3d7a,#2563eb)' : '#fff'};              color:${isMe ? '#fff' : '#1a2a3a'};              font-size:0.88rem;              font-family:Poppins,sans-serif;              box-shadow:0 1px 4px rgba(0,0,0,0.06);              word-break:break-word;            ">${m.text}</div>            <div style="font-size:0.65rem;color:#aab5c0;margin-top:3px;padding:0 4px;font-family:Poppins,sans-serif;">${time}</div>          </div>        </div>`;      }).join('');      box.scrollTop = box.scrollHeight;    });}// Send messagefunction sendMessage(e) {  e.preventDefault();  const input = document.getElementById('chatInput');  const text = input.value.trim();  if (!text || !chatPartner) return false;  if (chatPartner === currentUser) {    alert('You cannot message yourself.');    return false;  }  fetch(`${API_BASE}/messages`, {    method: 'POST',    headers: { 'Content-Type': 'application/json' },    body: JSON.stringify({ from: currentUser, to: chatPartner, text })  }).then(async res => {    const data = await res.json();    if (!res.ok || !data.success) {      throw new Error(data.message || 'Could not send message.');    }    input.value = '';    loadMessages();  }).catch(err => alert(err.message));  return false;}// Load inbox on page loadwindow.addEventListener('load', () => {  loadInbox();  loadNotifications_UI();  // Poll for new notifications every 30 seconds  setInterval(loadNotifications_UI, 30000);});// ══════════════════════════════════════════// NOTIFICATIONS UI// ══════════════════════════════════════════let notifDropdownOpen = false;function toggleNotifDropdown() {  const dd = document.getElementById('notifDropdown');  notifDropdownOpen = !notifDropdownOpen;  dd.style.display = notifDropdownOpen ? 'block' : 'none';  if (notifDropdownOpen) loadNotifications_UI();}// Close dropdown when clicking outsidedocument.addEventListener('click', (e) => {  const dd = document.getElementById('notifDropdown');  const btn = document.getElementById('notifBellBtn');  if (notifDropdownOpen && dd && btn && !dd.contains(e.target) && !btn.contains(e.target)) {    notifDropdownOpen = false;    dd.style.display = 'none';  }});async function loadNotifications_UI() {  if (!currentUser) return;  try {    const res = await fetch(`${API_BASE}/notifications/${encodeURIComponent(currentUser)}`);    const data = await res.json();    if (!data.success) return;    const notifs = data.notifications || [];    const unread = notifs.filter(n => !n.read).length;    // Update badge    const badge = document.getElementById('notifBadge');    if (unread > 0) {      badge.textContent = unread > 9 ? '9+' : unread;      badge.style.display = '';    } else {      badge.style.display = 'none';    }    // Render list    const listEl = document.getElementById('notifList');    if (!notifs.length) {      listEl.innerHTML = '<div style="text-align:center;color:#aab5c0;font-size:0.82rem;padding:30px 16px;font-family:Poppins,sans-serif;">No notifications yet</div>';      return;    }    listEl.innerHTML = notifs.slice(0, 30).map(n => {      const time = timeAgo(n.timestamp);      const icon = n.type === 'job-accepted' ? '✋' : n.type === 'job-completed' ? '✅' : '🔔';      const unreadDot = !n.read ? '<div style="width:8px;height:8px;border-radius:50%;background:#2563eb;flex-shrink:0;"></div>' : '';      const bg = !n.read ? 'background:#f0f6ff;' : '';      return `        <div onclick="handleNotifClick('${n.id}','${n.type}','${n.jobId || ''}')" style="display:flex;align-items:flex-start;gap:10px;padding:12px 18px;cursor:pointer;transition:background 0.15s;${bg}" onmouseover="this.style.background='#f4f7fb'" onmouseout="this.style.background='${!n.read ? '#f0f6ff' : 'transparent'}'">          <div style="font-size:1.2rem;flex-shrink:0;margin-top:2px;">${icon}</div>          <div style="flex:1;min-width:0;">            <div style="font-size:0.8rem;color:#1a2a3a;line-height:1.4;word-break:break-word;">${n.message}</div>            <div style="font-size:0.68rem;color:#8a9baa;margin-top:4px;">${time}</div>          </div>          ${unreadDot}        </div>`;    }).join('');  } catch (err) {    console.error('Error loading notifications:', err);  }}async function handleNotifClick(notifId, type, jobId) {  // Mark as read  try {    await fetch(`${API_BASE}/notifications/read`, {      method: 'PATCH',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ username: currentUser, ids: [notifId] })    });  } catch(e) {}  // Navigate to the relevant page  if (type === 'job-accepted' || type === 'job-completed') {    // Go to services/jobs page    const svcBtn = document.querySelector('[data-page=services]');    if (svcBtn) setPage(svcBtn);  }  // Close dropdown and refresh  notifDropdownOpen = false;  document.getElementById('notifDropdown').style.display = 'none';  loadNotifications_UI();}async function markAllNotifRead() {  if (!currentUser) return;  try {    await fetch(`${API_BASE}/notifications/read`, {      method: 'PATCH',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify({ username: currentUser })    });    loadNotifications_UI();  } catch(e) {}}function timeAgo(ts) {  const diff = Date.now() - ts;  const sec = Math.floor(diff / 1000);  if (sec < 60) return 'Just now';  const min = Math.floor(sec / 60);  if (min < 60) return min + 'm ago';  const hr = Math.floor(min / 60);  if (hr < 24) return hr + 'h ago';  const d = Math.floor(hr / 24);  if (d < 7) return d + 'd ago';  return new Date(ts).toLocaleDateString();}    function saveSettings(e, options = {}) {    if (e) e.preventDefault();    const email = document.getElementById('settingsEmail').value.trim();    const password = document.getElementById('settingsPassword').value.trim();    if (!email) {      alert('Email is required.');      return false;    }    const payload = {      email,      currentRole: options.overrideRole || currentRole    };    if ((options.persistPassword ?? true) && password) {      payload.password = password;    }    fetch(`${API_BASE}/users/${encodeURIComponent(currentUser)}/settings`, {      method: 'PATCH',      headers: { 'Content-Type': 'application/json' },      body: JSON.stringify(payload)    })    .then(async res => {      const data = await res.json();      if (!res.ok || !data.success) {        throw new Error(data.message || 'Could not update settings.');      }      account = data.user;      profiles = account.roles;      currentRole = account.currentRole || currentRole;      document.getElementById('settingsPassword').value = '';      if (!options.silent) alert('Settings updated!');    })    .catch(err => {      if (!options.silent) alert(err.message);    });    return false;  }  /* Sign out function */  function logout() {    if (!confirm('Are you sure you want to sign out?')) return;    localStorage.removeItem('gighub_user');    localStorage.removeItem('gighub_role');    window.location.href = 'login.html';  }  // SERVICE DETAILS MODAL LOGIC  function openServiceDetails(serviceId) {    const svc = (typeof services !== 'undefined' ? services : []).find(s => s.id === serviceId);    if (!svc) return;    const modal = document.getElementById('serviceDetailsOverlay');    if (!modal) return;    const titleEl = document.getElementById('svcModalTitle');    const bodyEl = document.getElementById('svcModalBody');    if (titleEl) titleEl.textContent = `${svc.emoji || ''} ${svc.title}`;    if (bodyEl) bodyEl.innerHTML = `      <div><b>Category:</b> ${svc.cat}</div>      <div><b>Price:</b> ${svc.price}</div>      <div><b>Status:</b> ${svc.status.charAt(0).toUpperCase() + svc.status.slice(1)}</div>      <div><b>Posted by:</b> ${svc.createdBy.charAt(0).toUpperCase() + svc.createdBy.slice(1)}</div>      ${svc.desc ? `<div style="margin-top:10px;"><b>Description:</b><br>${svc.desc}</div>` : ''}    `;    modal.style.display = '';  }  function closeServiceDetails() {    const modal = document.getElementById('serviceDetailsOverlay');    if (modal) modal.style.display = 'none';  }  // Optional: close modal when clicking outside  const svcOverlay = document.getElementById('serviceDetailsOverlay');  if (svcOverlay) {    svcOverlay.addEventListener('click', function(e) {      if (e.target === this) closeServiceDetails();    });  }  </script></body></html>